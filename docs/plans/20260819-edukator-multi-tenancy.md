# Edukator: базовая многоарендность

## Overview

Приложение сегодня однопользовательское по построению: `buildServer` открывает
единственную базу при старте и держит соединение в замыкании, `profile` — одна
строка, родительский PIN лежит в переменной окружения, аутентификации нет вовсе.
Этого хватает домашней сети и не хватает, как только сервер доступен снаружи.

План строит базовую многоарендность: родитель заводится по email и паролю (закрытым
списком, через скрипт), ведёт нескольких детей, каждый ребёнок входит со своего
компьютера по одноразовой ссылке и занимается **в своей отдельной базе**. Плюс
честная очередь фоновой генерации между детьми, без которой второй ребёнок ломает
занятие первому.

Утверждённая спека: `docs/superpowers/specs/2026-08-19-edukator-multi-tenancy-design.md`.
Основная спека: `docs/superpowers/specs/2026-08-07-edukator-design.md`.

**Не входит:** настройка TLS/секретов/бэкапов на хостинге, Windows-агент вместо
Python-контроллера, переход codex на API-ключ, самостоятельная регистрация
родителей и восстановление пароля почтой, общий банк заданий между детьми.

## Context (from discovery)

- Ветка `worktree-multi-tenancy` от `develop` (5fcd096). Базовый прогон чист:
  60 файлов, 1127 тестов.
- **`buildServer` (`server/index.ts`)** открывает базу один раз через
  `openSessionDatabase`, снимает отпечаток inode (`fileIdentity`) и раздаёт
  соединение всем `register*Routes` вместе с `available`. При недоступности базы
  регистрируются `registerUnavailable*` — то есть состав маршрутов выбирается на
  старте навсегда.
- **`registerSessionRoutes` (`server/routes/session.ts`)** держит в замыкании не
  только базу: `reviewing` (:142), `pendingReviews` (:143), `retryTimers` (:144),
  восстановление незакрытых споров `openDisputes(db)` (:399) и ожидание их
  завершения (:412). Это состояние **на арендатора**, а не на процесс.
- **PIN** сегодня — `EDUKATOR_PARENT_PIN` через `readParentPin`, проверяется
  `verifyParentPin` (SHA-256 + `timingSafeEqual`) и защищает ровно два маршрута:
  `PUT /api/parents/computer-access` и
  `PUT /api/parents/runs/:runId/integrity/:itemId/approve`.
- **Бюджет codex** — два процессных объекта `codexConcurrency` (2 места) и
  `disputeConcurrency` (1 место). `budget.run` в воркере (:243) оборачивает
  `produce`, внутри которого генерация **и** проверка, до трёх заходов — то есть
  один «слот» это 2-6 настоящих вызовов `runCodexCli`.
- **Воркер** (`server/codex/worker.ts`) за один `runWarmupCycle` готовит босса,
  доливает все голодные темы до `QUEUE_TARGET` параллельным пулом и готовит
  персональные материалы. `WorkerHandle` умеет только `stop()` (:474).
- **Статика**: сервер отдаёт `index.html` явно только для `/parents`
  (`server/index.ts:552`); прочие пользовательские адреса ушли бы в 404.
- **Пороги покрытия** в `vitest.config.ts` заданы поимённо (:15-81); новые модули
  не попадут ни под один шаблон.
- **Контроллер** (`controller/`) ходит в `gate/status` без авторизации, опрашивает
  раз в 20 секунд (`config.py:21`); `npm test` гоняет только Vitest и его правок
  не увидит.
- **E2E** поднимают `buildServer` внутри процесса на временной базе; `npm start`
  запрещён.

## Development Approach

- **Testing approach**: TDD для чистой логики (KDF и разбор секретов, политика
  допуска, порядок разрешения аренды, фазы диспетчера, резерв квоты). Для
  каркаса, схемы, скриптов и I/O допустим обычный порядок — ровно как записано в
  `CLAUDE.md`.
- **Ни один тест не вызывает настоящий codex.** Шим `codex` в `PATH` и проверка
  отсутствия маркера вызова остаются во всех e2e.
- Комментарии, тексты ошибок и названия тестов — по-русски. Комментарий объясняет
  *почему*, а не *что*.
- Каждая задача доводится до конца перед переходом к следующей.
- **КРИТИЧНО: каждая задача обязана содержать новые или обновлённые тесты** для
  изменённого в ней кода — и на успешный путь, и на ошибочный. Тесты не
  опциональны, это часть чеклиста.
- **КРИТИЧНО: все тесты проходят до начала следующей задачи.** Исключений нет.
- **КРИТИЧНО: план обновляется, если объём меняется по ходу.**
- Обратная совместимость **не** требуется: беспарольный режим убирается
  сознательно (см. спеку, «Почему беспарольного режима не остаётся»).

## Testing Strategy

- **Юнит-тесты**: обязательны в каждой задаче (см. выше). Один файл на модуль,
  имена совпадают, временные базы через `mkdtempSync`.
- **E2E (Playwright)**: сценарии поднимают `buildServer` сами. Меняются UI и
  HTTP-контракты, поэтому e2e переписываются в задачах 20-21 и должны проходить
  до задачи проверки соответствия.
- **Контроллер**: у него отдельный гейт `npm run family:test` (pytest в его
  окружении) — `npm test` его правок не видит.
- Тест, проходящий и после порчи проверяемого кода, не считается тестом:
  сомневаетесь — испортите строку в `server/` и убедитесь, что он покраснел.

## Progress Tracking

- Отмечать выполненное `[x]` сразу, не пакетом.
- Новые обнаруженные задачи — с префиксом ➕.
- Блокеры и проблемы — с префиксом ⚠️.
- Обновлять план, если реализация разошлась с замыслом.

## What Goes Where

- **Implementation Steps** (`[ ]`): всё, что делается в этом репозитории.
- **Post-Completion** (без чекбоксов): ручная проверка, деплой, внешние системы.

## Implementation Steps

### Task 1: Схема и миграция `control.db`

- [x] создать `server/control-db.ts`: своя `CONTROL_SCHEMA_VERSION`, свой
      `PRAGMA user_version`, свои `migrate` и `validateSchema`, не связанные с
      `SCHEMA_VERSION` детской базы
- [x] завести таблицы `parents`, `parent_invites`, `parent_sessions`, `children`,
      `child_devices`, `codex_quota`, `login_attempts` — состав колонок по спеке,
      отметки времени в ISO, умолчания через `strftime('%Y-%m-%dT%H:%M:%fZ','now')`
- [x] включить `foreign_keys = ON` и WAL тем же способом, что и `openDatabase`,
      с проверкой результата прагмы
- [x] написать тесты: база создаётся, состав таблиц совпадает со списком,
      `user_version` выставлен, повторное открытие идемпотентно
- [x] написать тесты ошибочных путей: база новее приложения отвергается, база без
      версии с посторонним объектом отвергается
- [x] прогнать тесты — должны проходить до задачи 2

### Task 2: Хранение пароля и PIN

- [x] создать `server/secrets.ts`: `scrypt` с индивидуальной солью, хеш строкой
      `scrypt$N$r$p$соль$хеш`, чтобы параметры поднимались без ломки старых записей
- [x] ограничить длину пароля **до** KDF: длинный пароль не должен становиться
      способом занять процессор
- [x] добавить серверный pepper для PIN из окружения (в базе не лежит):
      шестизначных значений миллион, и дамп базы перебирается офлайн за секунды
- [x] расширить `server/parent-pin.ts`: формат 6-12 цифр и сравнение постоянного
      времени сохраняются, эталон берётся из хеша, а не из открытого значения
- [x] написать тесты: верный пароль и PIN проходят, неверные нет, разные соли дают
      разные хеши одного значения, разбор строки хеша устойчив к мусору
- [x] написать тесты: без pepper проверка PIN отказывает (fail-closed), а не
      молча принимает
- [x] прогнать тесты — должны проходить до задачи 3

### Task 3: Родители, приглашения, сессии в `control.db`

- [x] добавить в `server/control-db.ts` операции: завести родителя, выпустить
      приглашение, установить пароль по приглашению, войти по паролю, выйти
- [x] хранить только SHA-256 от 256-битных токенов `randomBytes`; погашение
      приглашения — `UPDATE ... WHERE token_hash = ? AND used_at IS NULL` внутри
      `transaction(...).immediate()`
- [x] реализовать сроки сессии: 30 дней бездействия по `last_seen_at` и
      абсолютный потолок 90 дней, оба проверяются на каждом обращении
- [x] реализовать глушение записи `last_seen_at`: не чаще раза в пять минут
- [x] написать тесты: полный путь приглашение → пароль → сессия; протухшее
      приглашение не принимается; два одновременных погашения дают одну сессию
- [x] написать тесты сроков: сессия по бездействию гаснет, по абсолютному потолку
      гаснет даже при активности
- [x] прогнать тесты — должны проходить до задачи 4

### Task 4: Дети и устройства в `control.db`

- [x] добавить операции: завести ребёнка (`status = 'provisioning'`), перевести в
      `ready`, вывести (`retired_at`), список детей родителя
- [x] добавить операции устройств: выпустить приглашение (`browser` или `agent`),
      погасить его атомарно, отозвать, список устройств ребёнка
- [x] реализовать инвалидацию: `disabled_at` родителя, `credentials_changed_at`,
      `retired_at` ребёнка и `revoked_at` устройства проверяются **на каждом
      обращении**, а не при выдаче
- [x] путь базы ребёнка вычислять из непрозрачного `id`, канонизировать и
      проверять принадлежность каталогу `children/` — колонки `db_file` нет
      намеренно
- [x] написать тесты: два одновременных POST по одному приглашению устройства
      заводят ровно одно устройство
- [x] написать тесты инвалидации: каждое из четырёх событий гасит ровно то, что
      должно, и не гасит лишнего
- [x] написать тесты: `id` с `..` или разделителем пути не выводит за каталог
- [x] прогнать тесты — должны проходить до задачи 5

### Task 5: Суточная квота вызовов codex

- [x] добавить в `server/control-db.ts` резерв квоты: `CODEX_DAILY_QUOTA = 60`,
      московские сутки, атомарный `INSERT ... ON CONFLICT` с проверкой предела
      внутри `transaction(...).immediate()`
- [x] считать и неудачные вызовы: иначе зацикливание на ошибках обходит защиту,
      ради которой квота и заводится
- [x] написать тест «держит калибровочные константы спеки» с числом 60, вписанным
      руками
- [x] написать тесты: два параллельных резерва на границе предела пропускают
      только один; сутки сменяются по московской полуночи, а не по UTC
- [x] прогнать тесты — должны проходить до задачи 6

### Task 6: Каталог данных вместо одного пути

- [x] ввести `EDUKATOR_DATA_DIR`: `control.db` и `children/<id>.db`; убрать
      `EDUKATOR_DB` из `server/db.ts` и всех вызовов
- [x] снять с `databasePath()` роль глобальной точки — сегодня к ней ходят
      `buildServer`, `/api/health` и `syncCurriculumState`
- [x] завести создание базы ребёнка по протоколу `provisioning`: временный файл,
      `fsync`, `rename`, затем `ready`; повторный запуск продолжает незавершённое,
      а не отказывается вслепую
- [x] написать тесты: каталог создаётся, база ребёнка получает актуальную схему,
      прерванное заведение продолжается с обеих точек обрыва
- [x] написать тест: ребёнок в `provisioning` не выдаётся ни маршрутам, ни воркеру
- [x] прогнать тесты — должны проходить до задачи 7

➕ `databasePath()` удалён целиком: `buildServer` получил обязательный
`options.dbPath`, `checkDatabase` и `syncCurriculumState` — обязательный аргумент,
`openDatabase` — тоже. Все тесты и `e2e/harness.ts` переведены с `EDUKATOR_DB` на
явный путь.
➕ `legacySessionDatabasePath()` — времянка для `npm start`: до задачи 15
`buildServer` держит одно соединение занятия, и рабочему запуску путь брать
неоткуда. Задача 15 её убирает.
➕ `prefetch` теперь требует `--db`: умолчание грело бы не того ребёнка.
Обязательный аргумент «какого ребёнка греть» доводится в задаче 18.
➕ `isChildServiceable`/`listServiceableChildren` в `control-db.ts` — единый
источник «кого можно обслуживать» для маршрутов (задача 13) и воркера (задача 17).
➕ `syncDirectory` экспортирован из `atomic-write.ts`: заведение базы ребёнка
завершается тем же `rename` и обязано доносить его до диска так же.

### Task 7: Реестр детских баз

- [x] создать `server/tenant-registry.ts`: `open(childId)` с кешем, замком на
      первое открытие, `openSessionDatabase` (двойной замер inode — свой на каждую
      базу), однократными `syncTopicState` и посевом банка
- [x] **не** реализовывать вытеснение: потолок числа открытых баз отказывает
      новому арендатору и пишет об этом, но не закрывает чужое соединение
      исподтишка (закрытие под работающей проверкой даёт `database connection is
      not open`, а забытый отпечаток inode принял бы подменённый файл за прежний)
- [x] проверять потолок как положительное целое: на нуле он неотличим от «детей
      нет»
- [x] написать тесты: два одновременных первых обращения дают одну миграцию и
      один посев; отпечаток сохраняется; подмена файла краснит именно этого ребёнка
- [x] написать тесты: достижение потолка отказывает новому и **не** трогает уже
      открытых
- [x] прогнать тесты — должны проходить до задачи 8

➕ `openSessionDatabase`, `fileIdentity` и `SessionDatabase` переехали из
`server/index.ts` в `server/tenant-registry.ts` вместе с их тестами: реестр
обязан открывать базы тем же кодом, а импорт из `index.ts` дал бы кольцо, как
только задача 15 заведёт реестр в `buildServer`.

### Task 8: Спорное состояние — на арендатора

- [x] вынести из замыкания `registerSessionRoutes` состояние спора (`reviewing`,
      `pendingReviews`, `retryTimers`, восстановление `openDisputes`, ожидание
      завершения) в координатор арендатора рядом с координатором integrity
- [x] восстанавливать незакрытые споры при открытии **каждой** базы, а не один
      раз при старте сервера
- [x] остановку сервера сделать обходом координаторов всех открытых арендаторов
- [x] написать тесты: спор одного ребёнка не виден координатору другого; вердикт
      уходит в ту базу, где спор открыт
- [x] написать тест: перезапуск с незакрытым спором восстанавливает его в нужной
      базе
- [x] прогнать тесты — должны проходить до задачи 9

➕ `server/dispute-coordinator.ts` — новый модуль: `DisputeCoordinator` с
`schedule`/`restore`/`stop`, туда же переехали `DISPUTE_RETRY_MAX_MS`,
`BackgroundRunner` и первая пауза повтора `DISPUTE_RETRY_MS`.
`registerSessionRoutes` теперь принимает готовый координатор и ничего не
возвращает; `TenantRegistry.open` заводит координатор на аренду и зовёт
`restore()`, а `close`/`closeAll` стали асинхронными — разбор дожидается до
закрытия соединения. `buildServer` собирает координатор сам: до задачи 15 база
там одна, но код уже общий с реестром.

### Task 9: Разрешение предъявителя и аренды

- [x] создать `server/auth.ts`: три вида предъявителя — `parent` (cookie
      `__Host-edu_parent`), `browser` (cookie `__Host-edu_child`), `agent` (токен
      заголовком). Агент **не** детская сессия
- [x] реализовать жёсткий порядок: разобрать предъявителя → **одной выборкой**
      проверить принадлежность `children.id`, `status = 'ready'`, `retired_at IS
      NULL` → и только затем `open(childId)`
- [x] сделать «не ваш ребёнок» и «нет такого ребёнка» неотличимыми снаружи: один
      код, один текст (`runId` и `itemId` нумеруются в каждой базе с нуля, и ошибка
      выбора базы кончается не 404, а успешной операцией над чужой строкой)
- [x] выбирать детский принципал, если пришли обе cookie сразу
- [x] требовать совпадения `Origin` или `Sec-Fetch-Site: same-origin` на всех
      изменяющих запросах: `SameSite` защитой от CSRF не считается
- [x] написать тесты изоляции: родитель A → ребёнок B и ребёнок A → ребёнок B
      отказываются, база при этом **не открывается**
- [x] написать тесты: агент не проходит на обычные детские маршруты; обе cookie
      дают детский принципал; изменяющий запрос без обоих заголовков отклоняется
- [x] прогнать тесты — должны проходить до задачи 10

➕ `server/auth.ts` держит и коды с текстами отказа (`AUTH_STATUS`,
`AUTH_MESSAGE`): маршруты задач 11-13 обязаны отвечать на «не ваш ребёнок» одним
и тем же, а своя таблица в каждом маршруте разъехалась бы молча.
➕ Агентский токен идёт схемой `Authorization: Bearer`, а не своим заголовком:
контроллеру (задача 22) так не нужен ни свой разбор, ни cookie. Вид устройства
сверяется с каналом — агентский токен в детской cookie и детский в заголовке
предъявителями не служат.
➕ Недействительная детская cookie родительскую сессию не отменяет: родитель
настраивает ребёнка с его же машины, и отозванное там устройство иначе закрывало
бы вход тому, кто его отозвал.

### Task 10: Защита от перебора

- [x] реализовать счётчики неудач на пароль и на PIN в `control.db`, отдельно по
      email и по адресу, с паузой после серии
- [x] сделать fail-closed: при недоступности счётчика вход не пускаем
- [x] брать адрес клиента с доверием к `X-Forwarded-For` **только от
      перечисленных адресов** (по умолчанию loopback): голое `trustProxy: true`
      не защита, а её отключение — клиент подставит новый заголовок на каждую попытку
- [x] написать тесты: серия неудач приводит к паузе, верный пароль после паузы
      проходит, счётчики по email и по адресу независимы
- [x] написать тест: `X-Forwarded-For` от недоверенного адреса не влияет на счётчик
- [x] прогнать тесты — должны проходить до задачи 11

➕ Порог по адресу выше почтового (`LOGIN_ADDRESS_FAILURE_LIMIT = 20` против
`LOGIN_EMAIL_FAILURE_LIMIT = 5`): за одним адресом стоит вся семья вместе с NAT,
и общий порог означал бы, что ошибившийся брат закрывает вход остальным.
➕ Пауза (`LOGIN_LOCKOUT_MS = 15 минут`) служит и сроком жизни счётчика: неудача
старше её начинает серию заново, иначе редкие опечатки за месяц складывались бы
в запрет входа. Неудача **внутри** паузы её продлевает — пересидеть запрет,
продолжая долбить, нельзя.
➕ Верный секрет гасит только почтовый счётчик: адресный остаётся и остывает
сам. Знание одного пароля не повод обнулять общий счёт машины, за которой может
сидеть и подбирающий.
➕ Адрес клиента вынесен в отдельный `server/client-address.ts`
(`normalizeAddress`, `readTrustedProxies`, `clientAddress`): цепочка
`X-Forwarded-For` разбирается справа налево до первого недоверенного, вид адреса
проверяет `isIP`, а неразобранный адрес сокета даёт общий ключ `unknown` —
без него неизвестный адрес обходил бы счётчик вовсе. Маршруты входа
подключат его в задаче 11.

### Task 11: Маршруты входа

- [x] создать `server/routes/auth.ts`: `POST /api/auth/parent/login`,
      `POST /api/auth/parent/logout`, `GET` и `POST /api/auth/parent/invite/:token`,
      `POST /api/auth/child/claim/:token`, `GET /api/auth/me`
- [x] сделать `GET /api/auth/parent/invite/:token` не гасящим приглашение:
      погашение только POST, иначе предпросмотр в мессенджере сожжёт ссылку
- [x] в `GET /api/auth/me` не отдавать ни токенов, ни email чужого родителя
- [x] выставлять cookie: `HttpOnly`, `Secure`, `Path=/`, префикс `__Host-`,
      `SameSite=Strict` родителю и `Lax` ребёнку; `Secure` снимается только явным
      `EDUKATOR_INSECURE_COOKIES=1`
- [x] написать тесты успешных путей: вход, выход, установка пароля, погашение
      детской ссылки, ответ `me` для всех четырёх состояний
- [x] написать тесты ошибочных путей: чужой токен, протухший токен, повторное
      погашение, неверный пароль, погашение через GET
- [x] прогнать тесты — должны проходить до задачи 12
- ➕ погашение агентского приглашения отдаёт постоянный токен телом ответа и
      cookie не ставит: контроллеру он нужен строкой в файле настройки, и это
      единственный момент, когда токен виден вообще
- ➕ `EDUKATOR_INSECURE_COOKIES=1` читает `buildServer` в задаче 15: сам маршрут
      принимает выключатель параметром `insecureCookies`, чтобы тесты не зависели
      от окружения

### Task 12: Маршруты семьи

- [ ] создать `server/routes/family.ts`: `GET /api/family`,
      `POST /api/family/children`, `POST /api/family/children/:id/devices`,
      `POST /api/family/devices/:id/revoke`, `POST /api/family/pin`
- [ ] отдавать готовую ссылку-приглашение только в ответе на создание устройства —
      это единственный момент, когда токен виден вообще
- [ ] допускать сюда только родительскую сессию: ни PIN с детской машины, ни агент
- [ ] написать тесты: полный путь «завести ребёнка → выпустить ссылку → погасить →
      отозвать»; отозванное устройство получает отказ со следующего запроса
- [ ] написать тесты: детская сессия и агент не проходят ни на один маршрут семьи;
      родитель не видит и не трогает чужих детей
- [ ] прогнать тесты — должны проходить до задачи 13

### Task 13: Перевод существующих маршрутов на арендатора

- [ ] заменить во всех `server/routes/*.ts` источник `db`: контекст запроса вместо
      замыкания `buildServer`; доменные модули не трогать — они и так принимают `db`
- [ ] добавить `:childId` родительским маршрутам: `GET /api/parents/:childId`,
      `GET /api/parents/:childId/runs/:runId`,
      `PUT /api/parents/:childId/computer-access`,
      `PUT /api/parents/:childId/runs/:runId/integrity/:itemId/approve`
- [ ] реализовать матрицу допуска по спеке; детская сессия читает **свою** сводку
      без PIN (принципиальное ограничение основной спеки: ученик видит тот же
      дашборд и знает о нём), а два изменяющих маршрута требуют PIN
- [ ] родительской сессии PIN не спрашивать ни на одном действии
- [ ] допустить агента ровно на `GET /api/gate/status` и никуда больше
- [ ] написать тесты матрицы: каждая строка допуска проверяется и на «пускает», и
      на «не пускает»
- [ ] написать тесты: ребёнок читает свою сводку без PIN, но без PIN ничего не меняет
- [ ] прогнать тесты — должны проходить до задачи 14

### Task 14: Токены не утекают в логи

- [ ] заменять сегмент токена на `<token>` в том, что пишет `setErrorHandler`:
      сегодня он пишет полный `request.url`, а токен живёт в пути `/join/<token>`
- [ ] выставлять `Referrer-Policy: no-referrer` и `Cache-Control: no-store` на
      страницах и маршрутах погашения
- [ ] написать тесты: текст, уходящий в stderr, не содержит токена ни для
      `/join/:token`, ни для `/api/auth/parent/invite/:token`
- [ ] написать тест: заголовки выставлены на нужных ответах
- [ ] прогнать тесты — должны проходить до задачи 15

### Task 15: Сборка `buildServer`

- [ ] перевести `buildServer` на `control.db` и реестр: маршруты регистрируются
      один раз, соединение больше не живёт в замыкании
- [ ] сделать отказы точечными: карта тем и `control.db` гасят всё, испорченная
      база одного ребёнка — только его запросы, с указанием, чья база
- [ ] переписать `/api/health`: подробно проверять `control.db`, по **уже
      открытым** детям сверять отпечаток файла, не обходить неоткрытые базы с
      `quick_check`
- [ ] объявить список пользовательских адресов (`/`, `/parents`, `/join/:token`,
      `/invite/:token`) в одном месте и отдавать по ним `index.html`: сегодня
      явно отдаётся только `/parents`
- [ ] написать тесты: испорченная база одного ребёнка не мешает остальным;
      health не открывает того, к чему не обращались
- [ ] написать тест: каждый пользовательский адрес отдаёт `index.html`, забытый
      адрес виден как падающий тест, а не как мёртвая ссылка у ребёнка
- [ ] прогнать тесты — должны проходить до задачи 16

### Task 16: Квота в `CodexRunner`

- [ ] вложить списание квоты в `CodexRunner` — перед каждым фактическим
      `runCodexCli`, а не на `budget.run`: один `budget.run` оборачивает `produce`
      с двумя-шестью настоящими вызовами, а персональный материал — четырьмя и
      больше, и счётчик у семафора превратил бы предел 60 в 120-360
- [ ] оставить процессные `codexConcurrency` и `disputeConcurrency` теми же и там же
- [ ] фиксировать резерв до вызова, в своей короткой транзакции; сам вызов —
      строго вне всякой транзакции
- [ ] не трогать квотой споры и проверку осмысленности: они идут по запросу
      ученика, и молча ломать ему занятие квотой нельзя
- [ ] написать тест на батче, который делает два вызова: списывается два, а не один
- [ ] написать тесты: неудачный вызов списывает квоту; исчерпанная квота
      останавливает фон, но спор и проверка осмысленности проходят
- [ ] прогнать тесты — должны проходить до задачи 17

### Task 17: Фазы и диспетчер воркера

- [ ] разобрать цикл на фазы: `runWarmupCycle` получает целевой запас, предел
      батчей и флаги «готовить босса» и «готовить материал» — оба выполняются
      только в фазе излишков, иначе повторный заход запустит их второй раз
- [ ] реализовать диспетчер обхода детей: ученик за экраном первым, затем всем до
      `REFILL_BELOW`, затем добивать до `QUEUE_TARGET`; указатель обхода переживает цикл
- [ ] обслуживать только детей со свежим `children.last_activity_at`, обновляемым
      тем же глушёным путём, что и `last_seen_at`
- [ ] добавить `wake()` к `WorkerHandle`: сегодня он умеет только `stop()`, и
      вернувшийся ребёнок ждал бы конца минутной паузы или получасового отступа
- [ ] оставить отступ по `CodexUnavailableError` процессным, а `everyRefillFailed`
      считать по обходу всех детей
- [ ] написать тесты порядка: ученик за экраном обслуживается первым; ребёнок с
      тремя холодными темами не выедает цикл целиком; указатель не даёт последнему
      голодать
- [ ] написать тесты: босс и материал не готовятся дважды за два захода; `wake()`
      прерывает паузу; брошенный ребёнок выпадает из обхода и возвращается ответом
- [ ] прогнать тесты — должны проходить до задачи 18

### Task 18: Ручной прогрев не заводит второй бюджет

- [ ] ввести файловый замок в каталоге данных, который берут и сервер, и
      `scripts/prefetch.ts`
- [ ] заставить `prefetch` при живом сервере отказываться и называть причину:
      иначе это отдельный процесс со своей парой слотов — ровно тот сценарий,
      которым отвергнут вариант «процесс на ребёнка»
- [ ] сделать аргумент обязательным: какого ребёнка греть либо «всех активных»;
      молча греть первого попавшегося `prefetch` права не имеет
- [ ] написать тесты: замок занят — отказ с внятным текстом; замок свободен —
      работа идёт; аргумент обязателен
- [ ] прогнать тесты — должны проходить до задачи 19

### Task 19: Скрипты родителя, миграции и бэкапа

- [ ] создать `scripts/parent.ts`: завести родителя, выпустить приглашение,
      сменить PIN или пароль, отключить
- [ ] создать скрипт снятия копии базы через `VACUUM INTO`: `cp` под WAL даёт
      снимок без незакрытого журнала, то есть тихо испорченный прогресс
- [ ] создать `scripts/adopt-single-user.ts`: родитель, ребёнок, **копия**
      нынешней `edukator.db` через `VACUUM INTO` (оригинал остаётся откатом), с
      протоколом `provisioning → ready`
- [ ] отказываться от повторного запуска только для уже `ready`-ребёнка: иначе
      оборванная миграция стала бы невосстановимой
- [ ] написать тесты: миграция переносит прогресс целиком и не трогает оригинал;
      повторный запуск отказывается; прерванная попытка продолжается
- [ ] написать тесты бэкапа: копия открывается и проходит `validateSchema`
- [ ] прогнать тесты — должны проходить до задачи 20

### Task 20: Клиент — вход и семья

- [ ] добавить в `web/src/http.ts` отдельную трактовку 401: иначе каждый экран
      покажет «не удалось загрузить» вместо «войдите»
- [ ] создать `web/src/auth-api.ts` и `web/src/family-api.ts` — контракты держать
      там, а не собирать `fetch` в экранах
- [ ] развести `App.tsx` на четыре состояния: страница по ссылке (приглашение
      родителя или погашение детской), никто не вошёл, родитель, ребёнок
- [ ] добавить экраны: вход, установка пароля по приглашению, погашение детской
      ссылки, семья (дети, устройства, PIN)
- [ ] убирать токен из адресной строки через `history.replaceState` сразу после
      загрузки
- [ ] добавить переключатель ребёнка в родительскую сводку и убрать запрос PIN
      при родительской сессии
- [ ] написать тесты экранов на успешный путь и на ошибочный (неверный пароль,
      протухшая ссылка, отозванное устройство)
- [ ] прогнать тесты — должны проходить до задачи 21

### Task 21: E2E на многоарендности

- [ ] написать помощник, поднимающий `buildServer` на временном каталоге,
      заводящий родителя и ребёнка и отдающий cookie
- [ ] переписать пять сценариев (забег, спор, триаж, босс, `/parents`) на этот
      помощник
- [ ] добавить сценарий: родитель заводит ребёнка, выпускает ссылку, ребёнок её
      гасит и начинает забег
- [ ] добавить сценарий: родитель подтверждает забракованный вопрос с детской
      машины по PIN
- [ ] сохранить шим `codex` в `PATH` и явную проверку отсутствия маркера вызова
      после каждого сценария
- [ ] прогнать `npm run test:e2e` — должен проходить до задачи 22

### Task 22: Контроллер доступа на агентском токене

- [ ] добавить в `ControllerConfig` адрес сервера и агентский токен, поправить
      сериализацию, `fetch_gate` и сигнатуру `gate_reader`
- [ ] слать токен заголовком; логику fail-closed и работу с Family Safety не трогать
- [ ] сохранить права файла конфигурации `0600`: теперь в нём не только refresh
      token Microsoft, но и ключ от детского аккаунта
- [ ] завести `npm run family:test` (pytest в окружении контроллера) — `npm test`
      гоняет только Vitest и этих правок не увидит
- [ ] написать тесты: заголовок присутствует, токен не попадает в лог, 401 ведёт к
      fail-closed, права файла `0600`
- [ ] прогнать `npm run family:test` — должен проходить до задачи 23

### Task 23: Пороги покрытия для новых модулей

- [ ] добавить в `vitest.config.ts` шаблоны для `server/{control-db,secrets,auth,tenant-registry}.ts`
      и новых маршрутов: поимённые пороги заданы явно, и без правки 80% пофайлово
      для новых модулей осталось бы пожеланием
- [ ] прогнать `npm run coverage` — пороги должны выполняться

### Task 24: Verify acceptance criteria

- [ ] проверить, что реализовано всё из Overview и матрицы допуска спеки
- [ ] проверить изоляцию арендаторов на каждом маршруте: родитель A → ребёнок B,
      ребёнок A → ребёнок B
- [ ] проверить гонки: двойное погашение приглашения, параллельный резерв квоты на
      границе предела
- [ ] прогнать `npm test`
- [ ] прогнать `npm run test:e2e`
- [ ] прогнать `npm run family:test`
- [ ] прогнать `npm run typecheck`
- [ ] прогнать `npm run lint` — все замечания исправить
- [ ] прогнать `npm run coverage` — пороги 80% пофайлово

### Task 25: [Final] Обновить документацию

- [ ] обновить `CLAUDE.md`: `control.db` и его миграции, реестр арендаторов, три
      вида предъявителя, PIN только с детского устройства, квота в `CodexRunner`,
      `EDUKATOR_DATA_DIR` вместо `EDUKATOR_DB`, `family:test` в списке команд
- [ ] обновить `README.md`: первый запуск через `scripts/parent.ts`, миграция
      однопользовательской базы
- [ ] отметить в спеке, если реализация где-то разошлась с замыслом

## Technical Details

**Схема `control.db`** (своя версия, своя миграция):

| Таблица | Ключевое |
| --- | --- |
| `parents` | `email` уникальный в нижнем регистре, `password_hash`, `pin_hash`, `credentials_changed_at`, `disabled_at` |
| `parent_invites` | `token_hash`, `expires_at`, `used_at` |
| `parent_sessions` | `token_hash`, `last_seen_at`, `expires_at` |
| `children` | `status` (`provisioning`/`ready`/`failed`), `last_activity_at`, `retired_at`; **пути базы нет** |
| `child_devices` | `kind` (`browser`/`agent`), `invite_hash`, `claimed_at`, `token_hash`, `revoked_at` |
| `codex_quota` | `child_id`, `day` (Москва), `calls` |
| `login_attempts` | счётчики по email и по адресу |

**Три предъявителя:** `parent` (cookie `__Host-edu_parent`, `SameSite=Strict`),
`browser` (cookie `__Host-edu_child`, `SameSite=Lax`), `agent` (заголовок, только
`GET /api/gate/status`).

**Порядок разрешения запроса:** предъявитель → проверка принадлежности одной
выборкой → открытие базы. Не наоборот.

**Единица квоты:** один `runCodexCli`, не один `budget.run`.

**Константы:** `CODEX_DAILY_QUOTA = 60`, срок сессии 30 дней бездействия при
потолке 90, приглашение живёт 48 часов, детский cookie — десять лет при серверном
выключателе `revoked_at`.

## Post-Completion

*Без чекбоксов: требует ручных действий или внешних систем*

**Ручная проверка:**

- пройти путь целиком на живой машине: завести родителя скриптом, поставить
  пароль, завести ребёнка, выпустить ссылку, погасить её на втором компьютере,
  провести забег;
- проверить, что мигрированный прогресс сына совпадает с оригиналом (XP, темы,
  прогноз, лента забегов);
- проверить работу Family Safety-контроллера с агентским токеном на живом
  Microsoft-аккаунте — тесты его не вызывают.

**Внешние системы (этап 3, не входит в этот план):**

- обратный прокси с TLS и списком доверенных адресов для `X-Forwarded-For`;
- хранение pepper и прочих секретов на хостинге;
- расписание бэкапов детских баз через скрипт из задачи 19;
- закрыть прямой доступ к порту приложения снаружи прокси.
