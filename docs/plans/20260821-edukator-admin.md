# Edukator: админка оператора

## Overview

Приложение перестало быть домашним: `deploy.sh` ставит его systemd-сервисом на
VPS, и в `control.db` живут чужие семьи. У оператора нет ни одного способа узнать,
что там происходит, кроме ssh и `sqlite3` руками.

План строит админку оператора: четвёртый предъявитель `admin` со своим входом,
имперсонацию чужой семьи **в режиме только чтения** с двумя независимыми замками
на запись, трёхслойную статистику и собственное хранилище аварий вместо journald.

Утверждённая спека: `docs/superpowers/specs/2026-08-21-edukator-admin-design.md`.
Многоарендность: `docs/plans/20260819-edukator-multi-tenancy.md`.
Основная спека: `docs/superpowers/specs/2026-08-07-edukator-design.md`.

**Не входит:** второй фактор (место оставлено — вход разложен так, что шаг
вставляется без переделки маршрутов); роли и разные права у разных операторов;
любое редактирование чужих данных из админки; экспорт; ретеншен логов по времени;
счётчик забракованных генераций; графики — таблицы и числа; HTTP-журнал запросов
и события codex в логах.

## Context (from discovery)

- Ветка `worktree-multi-tenancy` от `develop`. Многоарендность уже на месте:
  `control.db` версии 1 с семью таблицами, три предъявителя, ленивый реестр
  арендаторов.
- **`server/auth.ts` (415 строк)** — `resolveBearer` разбирает предъявителя в
  жёстком порядке (детская cookie → `ACTOR_COOKIE` → агентский заголовок →
  родительская cookie), `authorizeChild` сверяет принадлежность **одной выборкой**
  до открытия базы, `resolveTenant` собирает всё вместе. `isMutating` и
  `assertSameOrigin` уже дают предикат «запрос меняет состояние», которым
  пользуется проверка источника.
- **`server/routes/tenant-context.ts`** — матрица `ROUTE_ACCESS` (`child`, `gate`,
  `dashboard`), `allow` обязателен у каждого маршрута,
  `tests/tenant-context.test.ts` сверяет свою таблицу с `onRoute` поимённо.
- **`TenantOpener.open(childId, bearer)`** получает вид предъявителя не ради
  открытия, а ради будильника прогрева (`dispatcher.wake` только на `browser`).
- **`server/control-db.ts`** — `CONTROL_SCHEMA_VERSION = 1`, `migrateControl`
  внутри `transaction(...).immediate()` с перечитыванием `user_version`,
  `CONTROL_TABLES` сверяется тестом, `login_attempts.kind` ограничен
  `CHECK (kind IN ('password', 'pin'))`.
- **Логов нет вовсе:** `Fastify({ logger: false })` (`server/index.ts:203`),
  подробности ошибок уходят `process.stderr.write` из `setErrorHandler`, на VPS их
  забирает journald. `console.error` в `server/` и `scripts/` не встречается ни
  разу.
- **`server/routes/token-privacy.ts`** — `redactTokenUrl` уже заменяет сегмент
  токена на `<token>` в том, что уходит в stderr.
- **`server/backup.ts`** открывает детские базы голым `better-sqlite3` в
  `readonly` — то есть readonly-обход детских баз в проекте уже работает.
- **`APP_PAGES`** (`server/index.ts:84`) — `['/', '/parents', '/join/:token',
  '/invite/:token']`, покрыт тестом; забытый адрес даёт 404 статики, а не поломку
  сборки.
- **`web/src/App.tsx`** держит четыре состояния (страница по ссылке, никто не
  вошёл, родитель, ребёнок); с админкой их станет шесть, а имперсонация ложится
  поперёк как признак.
- **Пороги покрытия** в `vitest.config.ts` заданы поимённо; новые модули не
  попадут ни под один шаблон.
- **`scripts/parent.ts`** читает секреты со stdin через `createSecretReader` —
  один `readline` на запуск, закрытый stdin даёт отказ, а не молчаливый успех.

## Development Approach

- **Testing approach**: TDD для чистой логики (разрешение админского
  предъявителя, оба замка имперсонации, ротация и разбор хвоста лога, агрегация
  статистики, выбор состояния клиента). Для схемы, маршрутов, скриптов и I/O
  допустим обычный порядок — ровно как записано в `CLAUDE.md`.
- **Ни один тест не вызывает настоящий codex.** Шим `codex` в `PATH` и проверка
  отсутствия маркера вызова остаются во всех e2e.
- Комментарии, тексты ошибок и названия тестов — по-русски. Комментарий объясняет
  *почему*, а не *что*.
- Каждая задача доводится до конца перед переходом к следующей.
- **КРИТИЧНО: каждая задача обязана содержать новые или обновлённые тесты** для
  изменённого в ней кода — и на успешный путь, и на ошибочный. Тесты не
  опциональны, это часть чеклиста.
- **КРИТИЧНО: все тесты обязаны проходить до начала следующей задачи.**
- **КРИТИЧНО: план обновляется, если по дороге меняется объём работы.**
- Схема **детской** базы не меняется ни в одной задаче. Это свойство держится
  намеренно: админка не трогает ни одной доменной таблицы.

## Testing Strategy

- **Unit-тесты** — обязательны в каждой задаче.
- **Калибровочные числа** — с буквальным тестом: тест, строящий ожидание из той
  же константы, её подмену не ловит.
- **Два замка имперсонации проверяются по отдельности.** Тест, остающийся зелёным
  после снятия любого одного из них, не держит ничего: весь смысл второго замка в
  том, что он переживает забытый флаг.
- **E2E** (Playwright) — один сценарий «оператор» в задаче 21; изменения UI и
  HTTP-контрактов требуют `npm run test:e2e` по правилу проекта.
- **Покрытие** — 80% пофайлово, пороги прописываются поимённо в задаче 22.

## Progress Tracking

- Отмечать выполненное `[x]` сразу, а не пачкой.
- Новые обнаруженные задачи — с префиксом ➕.
- Помехи и блокировки — с префиксом ⚠️.
- Расхождение с замыслом записывать разделом «Где реализация разошлась с
  замыслом» в конце файла.

## Implementation Steps

### Task 1: Схема `control.db` версии 2

- [x] поднять `CONTROL_SCHEMA_VERSION` до 2 и добавить переход с версии 1 в
      `migrateControl` — DDL целиком внутри существующей `transaction(...).immediate()`
- [x] завести таблицы `admins`, `admin_sessions`, `admin_impersonations`,
      `admin_audit`; отметки времени в ISO, умолчания через
      `strftime('%Y-%m-%dT%H:%M:%fZ','now')`
- [x] пересоздать `login_attempts` с `CHECK (kind IN ('password','pin','admin'))`
      **с переносом строк** — обнулить счётчики миграцией значит открыть окно
      перебора в предсказуемое время, в момент обновления
- [x] дополнить `CONTROL_TABLES` и таблицы состава колонок/проверок, которые
      сверяет `validateControlSchema`
- [x] написать тест обновления с версии 1: таблицы появились, строки
      `login_attempts` уцелели, `user_version` стал 2
- [x] написать тесты ошибочных путей: база версии 3 отвергается, повторная
      миграция идемпотентна
- [x] прогнать тесты — должны проходить до задачи 2

### Task 2: Админ в управляющей базе

- [x] добавить в `server/control-db.ts` операции админа: заведение, установка
      пароля, отключение, чтение по email — пароль через существующий
      `hashSecret`/`verifySecret`
- [x] завести `MIN_ADMIN_PASSWORD_LENGTH = 16` и проверять длину **до** KDF, как
      это сделано у родителя
- [x] завести `ADMIN_SESSION_IDLE_MS = 30 * 60 * 1000` и
      `ADMIN_SESSION_MAX_MS = 8 * 60 * 60 * 1000`, создание и разрешение сессии по
      `token_hash` с подновлением `last_seen_at` не чаще `SESSION_TOUCH_MS`
- [x] гасить сессии по `credentials_changed_at` и `disabled_at` одним текстом SQL
      и в проверке, и в погашающем `UPDATE`
- [x] научить счётчик перебора третьему виду `kind = 'admin'` — общий с
      родительским счётчик означал бы, что перебор чужого родительского пароля
      запирает вход оператору
- [x] написать тесты: вход верным паролем, отказ неверным, отказ отключённому,
      истечение по бездействию и по потолку, гашение сменой пароля
- [x] написать тест «держит калибровочные константы спеки» с числами 16, 30 минут,
      8 часов, вписанными руками
- [x] прогнать тесты — должны проходить до задачи 3

### Task 3: Аудит действий оператора

- [x] добавить запись и чтение `admin_audit`: `admin_id`, `at`, `action`,
      `child_id`, `parent_id`, `detail`
- [x] закрытый список значений `action` объединением типов, а не свободной
      строкой: фильтр имеет смысл, только если множество конечно
- [x] чтение — страницей с курсором по `at`, новые сверху
- [x] написать тесты записи и чтения, включая порядок и границу страницы
- [x] написать тест на закрытый список `action` с именами, вписанными руками
- [x] прогнать тесты — должны проходить до задачи 4

### Task 4: Разрешение админского предъявителя

- [x] добавить `admin` в `BearerKind` и строку `admin: ['admin']` в `ROUTE_ACCESS`
      — ни одна существующая строка не пополняется
- [x] написать `resolveAdminBearer` в `server/auth.ts`: смотрит только
      `__Host-edu_admin` и об имперсонации не знает
- [x] написать `createAdminContext` в `server/routes/tenant-context.ts`: у
      админских маршрутов **аренды нет вовсе**, значит `resolveTenant` им не нужен
- [x] сохранить проверку источника у изменяющих админских маршрутов
      (`assertSameOrigin`)
- [x] написать тесты: админская cookie пускает, родительская и детская — нет,
      отсутствие cookie даёт 401, отключённый админ — 401
- [x] дополнить `tests/tenant-context.test.ts` админскими маршрутами и добавить
      проверку, что ни одна существующая строка матрицы не пополнилась значением
      `admin`
- [x] прогнать тесты — должны проходить до задачи 5

### Task 5: Маршруты входа оператора

- [x] создать `server/routes/admin/auth.ts`: `POST /api/auth/admin/login`,
      `POST /api/auth/admin/logout`
- [x] ставить cookie `__Host-edu_admin` с `SameSite=Strict`, `HttpOnly`, `Secure`
      (снимается только существующим `EDUKATOR_INSECURE_COOKIES=1`)
- [x] списывать неудачу в счётчик перебора по обоим scope (email и адрес клиента)
      и отвечать одинаково на «нет такого админа» и «неверный пароль»
- [x] писать в `admin_audit` вход и отказ входа
- [x] написать тесты маршрутов: вход, выход, отказ, локаут после предела, ответ
      неразличим для несуществующего и существующего адреса
- [x] написать тест: изменяющий вход без подтверждённого источника отклоняется
- [x] прогнать тесты — должны проходить до задачи 6

### Task 6: Слой 1 статистики — только `control.db`

- [x] создать `server/admin/overview.ts`: родителей и детей всего и за 7/30 дней,
      застрявшие (`provisioning` дольше часа, `failed`), расход `codex_quota` за
      московские сутки по ребёнку и суммой, живые сессии и устройства, текущие
      локауты, размеры баз и свободное место в каталоге данных
- [x] ни одной детской базы не открывать: это главный экран, он обязан отвечать и
      тогда, когда с детскими базами что-то не так
- [x] создать `server/routes/admin/overview.ts` с `GET /api/admin/overview`
- [x] написать тесты на нескольких родителях и детях, включая застрявших и
      отключённых
- [x] написать тест: недоступность детских баз на ответ не влияет
- [x] прогнать тесты — должны проходить до задачи 7

### Task 7: CLI оператора

- [x] создать `scripts/admin.ts` с `create|password|disable`, по образцу
      `scripts/parent.ts`
- [x] читать пароль со stdin через тот же `createSecretReader` — один `readline` на
      запуск; аргументы видны в `ps` и остаются в истории оболочки
- [x] закрытый stdin — отказ с ненулевым кодом, а не молчаливый успех
- [x] `create` заводит управляющую базу, остальные команды идут с `fileMustExist`:
      опечатка в `--data-dir` не должна заводить пустую базу рядом
- [x] добавить `npm run admin` в `package.json`
- [x] написать тесты исполняемой заглушкой по образцу тестов `scripts/parent.ts`:
      коды возврата, закрытый stdin, две строки пароля из одной трубы
- [x] прогнать тесты — должны проходить до задачи 8

### Task 8: Клиент — вход и главный экран админки

- [x] создать `web/src/admin-api.ts` — единственное место HTTP-контракта админки
- [x] создать `web/src/admin/AdminLoginScreen.tsx` отдельно от `LoginScreen`:
      публичный корень не должен предлагать форму входа оператора
- [x] создать `web/src/admin/AdminHomeScreen.tsx` — цифры слоя 1 и список семей с
      детьми, статусами и последней активностью
- [x] добавить `/admin` в `APP_PAGES` и продублировать минимум пароля числом 16 в
      клиенте (импортировать из `server/` нельзя)
- [x] написать тесты экранов: загрузка, 401 показывает вход, а не поломку, ошибка
      сети даёт «Повторить»
- [x] написать тест копии минимума пароля с числом, вписанным руками, — поднятый
      на сервере минимум оставил бы клиент, молча принимающий короткий пароль
- [x] дополнить тест `APP_PAGES`
- [x] прогнать тесты и `npm run build:web` — должны проходить до задачи 9
- [x] ➕ дополнить слой 1 списком семей (`families` в `AdminOverview`): цифры
      задачи 6 списка семей не содержали, а рисовать его вторым запросом значило
      бы показывать цифры и семьи на разные моменты времени. Тем же обходом
      `children`, что и размеры баз, ни одной детской базы не открывая
- [x] ➕ создать `web/src/admin/AdminApp.tsx` — корень админки, переключающий вход
      и главный экран: без него «401 показывает вход» некому исполнить, а
      отдельного `/api/admin/me` нет намеренно (живость сессии показывает первый
      же запрос за данными). `App.tsx` подключит его в задаче 21

### Task 9: Хранилище аварий

- [x] создать `server/log.ts` с `logFailure({ event, message, detail?, childId?,
      route?, status? })` — синхронная дозапись строки JSONL
- [x] `event` — закрытое объединение типов: `server-error`, `tenant-open-failed`,
      `tenant-detached`, `control-error`, `startup-failed`, `codex-unavailable`,
      `codex-run-failed`, `sweep-failed`, `prefetch-failed`, `backup-failed`,
      `login-lockout`
- [x] писать в `<каталог данных>/logs/app.jsonl` с ротацией по размеру:
      `LOG_MAX_BYTES = 8 * 1024 * 1024`, `LOG_KEEP_FILES = 4` — потолок в 32 МБ и
      есть весь ретеншен, лог лежит на одном диске с детскими базами
- [x] пропускать сообщение через существующую `redactTokenUrl`; тел запросов и
      содержимого ответов ребёнка не писать вовсе
- [x] отказ самой записи лога не имеет права уронить запрос: он уходит в stderr и
      на этом заканчивается
- [x] написать тесты: строка пишется и разбирается, ротация на границе размера,
      старые файлы уходят за `LOG_KEEP_FILES`, токен заменён на `<token>`
- [x] написать тест «держит калибровочные константы спеки» с числами 8 МБ и 4
- [x] ➕ завести `redactTokenText` в `server/routes/token-privacy.ts` на том же
      `TOKEN_PATH_PREFIXES`: `redactTokenUrl` берёт адрес целиком и потому умеет
      сохранить строку запроса, но токен в журнале лежит внутри фразы («не удалось
      погасить /join/<токен>: причина»), и на таком тексте она не срабатывает
      вовсе. Адрес (`route`) по-прежнему идёт через `redactTokenUrl`
- [x] прогнать тесты — должны проходить до задачи 10

### Task 10: Перевод существующих аварий на журнал

- [x] `setErrorHandler` в `server/index.ts` пишет и в stderr, и в журнал: процесс,
      упавший до инициализации лога, виден только в stderr
- [x] `server/tenant-registry.ts` — отказ открытия, подмена файла, отказ миграции
- [x] `server/codex/` — недоступность и отступ диспетчера
- [x] старт сервера — отказ карты тем и `control.db`
- [x] доменную логику не менять ни в одном модуле: добавляется вызов там, где уже
      стоит `process.stderr.write`
- [x] написать тесты: авария каждого вида доезжает до файла с ожидаемым `event`
- [x] прогнать тесты — должны проходить до задачи 11
- [x] ➕ завести `FailureLog` и `failureLogFor(dir)` в `server/log.ts`: каталог
      данных знает не всякий модуль, а умолчание в `dataDir()` писало бы из
      каждого теста в `data/` рядом с кодом. Реестр каталог знает и получает
      настоящее умолчание; `registerErrorHandler` и `WarmupDispatcher` берут
      журнал **обязательным** аргументом — забытая передача обязана падать на
      сборке, а не терять аварию молча
- [x] ➕ подмена файла и пропавший файл в реестре различаются тем, есть ли файл
      на месте после отказа `openSession`: `tenant-detached` и
      `tenant-open-failed` зовут к разным действиям, и одно имя на обе прятало бы
      первую

### Task 11: Чтение журнала и его маршрут

- [x] читать **хвост**: последние `LOG_TAIL_BYTES = 512 * 1024` текущего файла,
      при нехватке — предыдущий; целиком в память файл не читать
- [x] отбрасывать первую оборванную строку и битые строки поштучно: испорченная
      строка не имеет права закрыть весь экран
- [x] создать `server/routes/admin/logs.ts` с
      `GET /api/admin/logs?event=&child=&before=`, не больше
      `ADMIN_LOG_PAGE = 200` записей
- [x] написать тесты: хвост, обрезка первой строки, фильтры, курсор `before`,
      переход через границу файлов
- [x] написать тест «держит калибровочные константы спеки» с числами 512 КБ и 200
- [x] прогнать тесты — должны проходить до задачи 12
- [x] ➕ курсор `before` — не одна отметка времени, а `<отметка>#<сколько записей
      с этой отметкой уже отдано>`: аварии приходят пачкой, и несколько записей
      одной миллисекунды — обычное дело. Строгое «раньше чем» теряло бы соседей
      по границе страницы, нестрогое повторяло бы их вечно. Плоскую отметку
      маршрут по-прежнему принимает — это тот же курсор с нулём
- [x] ➕ хвост читается с одним байтом сверх бюджета: только по байту перед
      срезом видно, разрубил он строку или лёг ровно на её начало, и без него
      целая запись выбрасывалась бы всякий раз, когда граница совпала с
      переводом строки
- [x] ➕ неизвестное `event` в запросе — 400, а не пустая лента: пустой ответ на
      опечатку в фильтре читался бы как «аварий такого рода не было»

### Task 12: Клиент — лента аварий

- [x] создать `web/src/admin/AdminLogsScreen.tsx`: лента с фильтром по событию и
      по ребёнку, строка раскрывается в подробности
- [x] добавить контракт в `web/src/admin-api.ts`
- [x] написать тесты экрана: загрузка, пустой журнал, фильтр, подгрузка по курсору
- [x] прогнать тесты и `npm run build:web` — должны проходить до задачи 13
- [x] ➕ продублировать на клиенте закрытый список событий (`ADMIN_LOG_EVENTS`):
      импортировать `server/` клиенту нечем, а без списка у фильтра нет ни
      одного варианта на пустом журнале — отфильтровать можно было бы ровно то,
      что и так видно. Синхронность копии держит `tests/log.test.ts` сверкой с
      текстом `web/src/admin-api.ts`: разъехавшийся список не роняет ничего, он
      прячет от оператора целый род аварий
- [x] ➕ фильтр по ребёнку применяется кнопкой, а не по знаку ввода: набранный
      фильтр отделён от применённого (`applied`), иначе хвост журнала
      перечитывался бы по разу на букву идентификатора
- [x] ➕ подключить ленту к `AdminApp` состоянием `page` вместо второго адреса:
      `/admin` — единственная страница админки в `APP_PAGES`, и вторая ссылка
      ломалась бы 404 статики ровно в тот день, когда ленту открывают. Сводка
      получила кнопку «Аварии», лента — «К сводке»
- [x] ➕ страницы **дописываются** к прочитанному, а смена фильтра начинает ленту
      заново: курсор `before` шагает от последней отданной записи, и заменой
      оператор терял бы всё, что успел прочитать выше, а дописанное к
      нефильтрованному было бы ответом на два разных вопроса сразу

### Task 13: Сессия имперсонации в управляющей базе

- [x] добавить операции `admin_impersonations`: старт, разрешение по токену,
      закрытие; токен — 256-битный `randomBytes`, в базе только SHA-256
- [x] `IMPERSONATION_TTL_MS = 15 * 60 * 1000`
- [x] старт гасит предыдущую живую имперсонацию того же админа тем же `UPDATE`:
      иначе «в чьей я семье» становится вопросом к вкладке браузера
- [x] гасить живые имперсонации по `credentials_changed_at` и `disabled_at` — тем
      же текстом SQL, что и сессии
- [x] разрешать только `browser` и `parent`; целевой ребёнок проверяется через
      `isChildServiceable`
- [x] написать тесты: старт, разрешение, истечение, закрытие, вытеснение
      предыдущей, гашение сменой пароля, отказ на `agent` и на необслуживаемого
      ребёнка
- [x] написать тест «держит калибровочные константы спеки» с числом 15 минут
- [x] прогнать тесты — должны проходить до задачи 14

### Task 14: Предъявитель имперсонации и первый замок

- [x] добавить необязательное поле `impersonation: { adminId, expiresAt }` в
      `Bearer`
- [x] добавить четвёртую ветку в `resolveBearer`, проверяемую **первой** на пути к
      аренде; возвращает обычный `{ kind: 'browser' | 'parent', ... }` целевой
      семьи, а не новый вид — новый `BearerKind` пришлось бы вписывать в каждую
      строку матрицы
- [x] добавить `AuthErrorCode` `read-only` (403) с текстом «Только просмотр: вы в
      чужой семье» в `AUTH_STATUS` и `AUTH_MESSAGE`
- [x] отказывать в `resolveTenant` при `isMutating(method) || mutating === true`
      для имперсонированного предъявителя
- [x] написать тесты: чтение проходит, `POST` отказывает, помеченный `mutating`
      `GET /api/session/next` отказывает тоже — он списывает задание из банка
      безвозвратно
- [x] написать тесты: имперсонация выигрывает у собственной родительской cookie
      оператора, истёкшая не пускает, закрытая не пускает
- [x] прогнать тесты — должны проходить до задачи 15

### Task 15: Второй замок — соединение только для чтения

- [x] выдавать имперсонированному контексту **своё** соединение к базе ребёнка с
      `PRAGMA query_only = ON`, открытое тем же `openDatabase`: не общий handle
      реестра (`query_only` — свойство соединения, на общем он запер бы запись
      самому ребёнку) и не голый readonly (различия — единственное, чего в точном
      воспроизведении быть не должно)
- [x] открывать аренду через реестр, чтобы миграция, посев и координаторы
      отработали так же, как для настоящего входа; маршруту отдавать второй handle
- [x] закрывать соединение при выходе из имперсонации, истечении TTL и закрытии
      сервера
- [x] сменить сигнатуру `TenantOpener.open` на
      `open(childId, bearer: { kind: BearerKind; impersonated: boolean })` и
      обновить все места вызова: признак обязан доехать до реестра явным полем,
      иначе имперсонация с видом `browser` молча будила бы прогрев
- [x] не подновлять `children.last_activity_at`, не звать `dispatcher.wake`, не
      двигать `last_seen_at` целевого родителя
- [x] написать тест **отдельно на второй замок**: прямая попытка записи по
      имперсонированному соединению падает на движке даже при снятом первом замке
- [x] написать тесты: прогрев не будится, отметки активности не двигаются,
      соединение закрывается на выходе
- [x] прогнать тесты — должны проходить до задачи 16
- ➕ выбор соединения вынесен из замыкания `buildServer` в
      `server/tenant-opener.ts` (`createTenantOpener`): ветка «пишущее соединение
      и будильник прогрева» и ветка «второй handle только для чтения» — две
      стороны одного вопроса «кто пришёл», и в замыкании сервера они не
      проверялись бы без поднятого сервера. Задаче 23 нужен порог покрытия и на
      этот файл.

### Task 16: Маршруты имперсонации

- [x] создать `server/routes/admin/impersonate.ts`: `POST /api/admin/impersonate`
      (`{ childId, role }`) и `DELETE /api/admin/impersonate`
- [x] ставить и снимать cookie `__Host-edu_impersonation` с `SameSite=Strict`
      рядом с админской, а не вместо неё: выход возвращает оператора в админку
- [x] писать в `admin_audit` начало и конец, в записи о конце — счётчик
      отказанных попыток записи
- [x] создать `server/routes/admin/audit.ts` с `GET /api/admin/audit`
- [x] написать тесты маршрутов: старт, выход, повторный старт гасит первый, чужой
      или необслуживаемый ребёнок даёт `no-child`, счётчик отказов доезжает до
      записи о конце
- [x] прогнать тесты — должны проходить до задачи 17
- ➕ счётчик отказов вынесен в `server/admin/impersonation-refusals.ts`, а
      `resolveTenant` получил необязательный `onReadOnly`: сам он между
      запросами не помнит ничего, а число обязано дожить до записи о конце.
      Счётчик процессный, не в `control.db`: запись на каждый отказ означала бы,
      что закрытый маршрут умеет заставить нас писать.
- ➕ `listAdminAudit` получил фильтр по действию (комментарий у
      `isAdminAuditAction` обещал его маршруту ещё в задаче 3) и константу
      `ADMIN_AUDIT_PAGE = 200` с буквальным тестом — размер страницы тот же, что
      у ленты аварий.
- ➕ `serializeCookie` получил четвёртую сторону `impersonation`
      (`SameSite=Strict`, срок равен `IMPERSONATION_TTL_MS`): политика cookie
      обязана оставаться одной, своя сборка `Set-Cookie` в новом маршруте
      разъехалась бы с прочими молча.

### Task 17: Клиент — баннер имперсонации

- [x] добавить в ответ `/api/auth/me` поле
      `impersonation: { adminEmail, childName, role, expiresAt }`
- [x] создать `web/src/admin/ImpersonationBanner.tsx` — несъёмная полоса поверх
      настоящих детских и родительских экранов: чья семья, сколько осталось,
      кнопка «выйти». Без неё скриншот из имперсонации неотличим от скриншота от
      родителя
- [x] разобрать `read-only` в `web/src/http.ts` отдельным случаем, как уже
      разбирается 401, и показывать его баннером, а не экраном поломки
- [x] добавить кнопки «войти как ребёнок / как родитель» в `AdminHomeScreen`
- [x] написать тесты: баннер виден на детском и родительском экране, отказ на
      запись показывается баннером, выход возвращает в админку
- [x] прогнать тесты и `npm run build:web` — должны проходить до задачи 18
- ➕ `failAuth` стал отдавать `code` рядом с текстом: «только просмотр»,
      «доступ закрыт» и «запрос пришёл не со страницы» отвечают одним 403, и по
      статусу клиент их не различает — то есть работающий замок показывался бы
      экраном поломки. Текст остаётся человеку, код — коду.
- ➕ `ImpersonationMark` понесла `adminEmail`, `childName` и `role`:
      `resolveImpersonation` их уже читает, а второй запрос за подписью показывал
      бы чужой экран нарисованным, но ещё неподписанным.
- ➕ `/api/auth/me` проверяет заход **раньше** собственных cookie: оператор
      заходит со своей машины, где живы обе его сессии, и ветка `both` вернула бы
      ему свою же семью — баннера не было бы ровно там, где он и нужен.
- ➕ `admin-api` получил `impersonate` и `stopImpersonation`: свой `fetch` в
      баннере и на главном экране разъехался бы с политикой админских запросов
      (401 не уводит на родительский вход).

### Task 18: Опенер детских баз для отчётов

- [x] создать `server/admin/child-readonly.ts`: открытие базы ребёнка голым
      `better-sqlite3` в `readonly`, по одной, с немедленным закрытием — реестра
      это не касается вовсе, поэтому потолок в 32 аренды не при чём
- [x] не мигрировать: ребёнок, не заходивший с прошлого обновления, честно
      показывается как «схема N, ждёт первого захода»
- [x] отказ одной базы не отменяет остальных — он попадает в `failed[]`, тот же
      принцип, что у `backupDataDir` и `prefetchChildren`
- [x] написать тесты: чтение исправной базы, база со старой схемой опознана и не
      изменена, битая база попала в `failed[]` и не сорвала обход
- [x] написать тест: `user_version` и содержимое базы после обхода не изменились
- [x] прогнать тесты — должны проходить до задачи 19

### Task 19: Слой 2 — агрегация по всем детям

- [x] создать `server/admin/stats.ts`: вовлечённость (активные сегодня/7/30,
      отвалившиеся по неделям от заведения, активное время по
      `attempts.duration_ms`, серии), учебная картина (завершённые забеги,
      точность, среднее mastery, пройденная калибровка, боссы, integrity-отказы,
      споры и доля выигранных), качество контента (расход вызовов codex против
      прироста банка, пустые банки по темам, темы с худшей точностью)
- [x] кеш на `ADMIN_STATS_TTL_MS = 5 * 60 * 1000` с отметкой времени в ответе;
      пересчёт — явным `?refresh=1`, а не каждым заходом
- [x] содержания не читать вовсе: ни текстов ответов, ни формулировок споров, ни
      персональных материалов — только состояние
- [x] написать тесты на нескольких временных детских базах, включая `failed`,
      застрявший `provisioning` и базу со старой схемой
- [x] написать тесты кеша: второй запрос базы не открывает, `refresh` открывает,
      отметка времени меняется
- [x] написать тест «держит калибровочные константы спеки» с числом 5 минут
- [x] прогнать тесты — должны проходить до задачи 20

### Task 20: Слой 3 и маршруты статистики

- [x] создать `server/admin/child-detail.ts`: банк по темам, очередь материалов,
      споры, боссы, состояние гейта — открывается только база названного ребёнка
- [x] создать `server/routes/admin/stats.ts` (`GET /api/admin/stats`,
      `?refresh=1`) и `server/routes/admin/children.ts`
      (`GET /api/admin/children/:childId`)
- [x] создать `web/src/admin/AdminStatsScreen.tsx` с отметкой «данные на 12:41» и
      кнопкой пересчёта и `web/src/admin/AdminChildScreen.tsx`
- [x] добавить `/admin/child/:childId` в `APP_PAGES` и дополнить его тест
- [x] написать тесты маршрутов: чужой формат `childId` даёт `no-child`, отчёт с
      `failed[]` отдаётся с признаком неполноты, а не пятисоткой
- [x] написать тесты экранов: загрузка, отметка времени, пересчёт, частичный отчёт
- [x] прогнать тесты и `npm run build:web` — должны проходить до задачи 21
- [x] ➕ починить время в `tests/auth.test.ts`: оператор заводился настоящими
      часами, а заход — зафиксированным `NOW`, и `credentials_changed_at` гасил
      его каждый раз, когда настоящее время обгоняло `NOW` (то есть тесты
      имперсонации краснели ежедневно после 10:00 UTC). Часы оператора и разбор
      предъявителя переведены на те же `NOW`

### Task 21: Вынос выбора состояния клиента

- [x] создать `web/src/app-route.ts` — чистая функция «адрес плюс ответ
      `/api/auth/me` → какое состояние рисовать»
- [x] перевести `App.tsx` на неё, оставив ему сборку; ничего сверх этого в
      существующем клиенте не переписывать
- [x] написать тесты на все состояния: страница по ссылке, никто не вошёл,
      родитель, ребёнок, админ, админ под имперсонацией
- [x] прогнать тесты и `npm run build:web` — должны проходить до задачи 22
- [x] ➕ подключить `web/src/admin/AdminApp.tsx` к `App.tsx`: корень админки был
      написан в задаче 8, но его никто не рисовал, и `/admin` отдавал оператору
      форму родительского входа. `me` на админском адресе не спрашивается вовсе —
      под заходом он назвал бы предъявителя чужой семьи, а не оператора

### Task 22: E2E-сценарий «оператор»

- [x] добавить в `tests/server-harness.ts` помощника заведения админа прямо в
      `control.db` — своя копия установки в новом тесте разъедется молча
      (`createAdminAccount` и `signInAdmin`: заведение и вход разделены, потому
      что сценарий входит формой и лишняя живая сессия испортила бы и сводку
      сессий, и журнал действий)
- [x] написать e2e: вход оператора, список семей, заход в чужого ребёнка, баннер,
      отказ на попытке ответить, выход, строка в аудите (`e2e/admin.spec.ts`;
      браузер сценария приходит без единой cookie семьи — чужие экраны
      открываются только заходом)
- [x] проверить в сценарии отсутствие маркера вызова настоящего codex, как во всех
      остальных
- [x] прогнать `npm run test:e2e` — должны проходить до задачи 23
- [x] ➕ перевести пять маршрутных тестов админки (`audit`, `children`, `logs`,
      `overview`, `stats`) на того же помощника: тройка
      `createAdmin`/`setAdminPassword`/`loginAdmin` была скопирована в каждый, и
      шестая копия в e2e разъехалась бы с ними молча
- [x] ➕ остаток срока на полосе захода сценарием не проверяется: сервер e2e
      живёт на зафиксированных часах, а полоса считает минуты настоящими —
      счётчик проверяется юнит-тестом `ImpersonationBanner.test.tsx`

### Task 23: Пороги покрытия для админки

- [x] добавить в `vitest.config.ts` шаблоны для `server/admin/**`,
      `server/routes/admin/**`, `server/log.ts`, `web/src/admin/**` и
      `web/src/app-route.ts`: пороги заданы поимённо, и без правки 80% пофайлово
      для новых модулей осталось бы пожеланием
- [x] ➕ добавить туда же `web/src/admin-api.ts` и внести новые клиентские файлы в
      `coverage.include`: список включений перечисляет клиента поимённо, и без
      этого шаблон `web/src/admin/**` не измерял бы вовсе ничего
- [x] ➕ дописать тесты под открывшиеся пороги: ветки `readChildDetail`
      (заполненные отметки времени, тема вне карты, время по умолчанию),
      пустые разделы и безымянная тема в `AdminChildScreen`, переходы корня
      админки (`web/src/admin/AdminApp.test.tsx`)
- [x] прогнать `npm run coverage` — пороги должны выполняться

### Task 24: Verify acceptance criteria

- [x] проверить, что реализовано всё из Overview и из спеки
- [x] проверить оба замка имперсонации **по отдельности**: снять один — второй
      обязан удержать запись
- [x] проверить, что схема детской базы не изменилась (версия 17, состав таблиц
      прежний)
- [x] проверить изоляцию: админские маршруты закрыты родителю, ребёнку и агенту;
      имперсонация чужого ребёнка чужим родителем невозможна
- [x] проверить, что имперсонация не двигает `last_activity_at` и не будит прогрев
- [x] прогнать `npm test`
- [x] прогнать `npm run test:e2e`
- [x] прогнать `npm run typecheck`
- [x] прогнать `npm run lint` — все замечания исправить
- [x] прогнать `npm run coverage` — пороги 80% пофайлово
- [x] прогнать `npm run build:web`

### Task 25: [Final] Обновить документацию

- [x] обновить `CLAUDE.md`: четвёртый предъявитель и его отдельное разрешение,
      имперсонация с двумя замками, `server/log.ts` и хранилище аварий, слои
      статистики, новая сигнатура `TenantOpener.open`, `npm run admin` в списке
      команд, `CONTROL_SCHEMA_VERSION = 2`
- [x] обновить `README.md`: заведение оператора, вход в админку, где лежат логи и
      сколько их хранится
- [x] записать разделом «Где реализация разошлась с замыслом», если реализация
      где-то разошлась со спекой

## Technical Details

**Новые таблицы `control.db`** (версия 2):

| Таблица | Ключевое |
| --- | --- |
| `admins` | `email` уникальный в нижнем регистре, `password_hash`, `credentials_changed_at`, `disabled_at` |
| `admin_sessions` | `token_hash`, `last_seen_at`, `expires_at`, `revoked_at` |
| `admin_impersonations` | `child_id`, `role` (`browser`/`parent`), `token_hash`, `expires_at`, `revoked_at` |
| `admin_audit` | `admin_id`, `at`, `action`, `child_id`, `parent_id`, `detail` |
| `login_attempts` | пересоздана: `kind` получил третье значение `admin` |

**Четвёртый предъявитель:** `admin` — cookie `__Host-edu_admin`,
`SameSite=Strict`, строка матрицы `admin: ['admin']`. Аренды у админских
маршрутов нет, поэтому их разрешает отдельный `createAdminContext`, а не
`resolveTenant`: приоритет имперсонации иначе вернул бы на `/api/admin/*`
предъявителя `browser`, и админка отказывала бы ровно тогда, когда оператор в
имперсонации и хочет из неё выйти.

**Имперсонация:** cookie `__Host-edu_impersonation`, живёт рядом с админской.
`resolveBearer` возвращает обычного `browser`/`parent` целевой семьи плюс поле
`impersonation`. Два независимых замка: отказ по `mutating` в `resolveTenant` и
`PRAGMA query_only = ON` на отдельном соединении.

**Слои статистики:** 1 — только `control.db`, мгновенно; 2 — обход детских баз по
одной в `readonly` с кешем; 3 — одна названная база по требованию.

**Журнал:** `<каталог данных>/logs/app.jsonl`, ротация по размеру, чтение хвостом.

**Константы:**

| Константа | Значение |
| --- | --- |
| `MIN_ADMIN_PASSWORD_LENGTH` | 16 |
| `ADMIN_SESSION_IDLE_MS` | 30 минут |
| `ADMIN_SESSION_MAX_MS` | 8 часов |
| `IMPERSONATION_TTL_MS` | 15 минут |
| `ADMIN_STATS_TTL_MS` | 5 минут |
| `CHURN_SILENCE_DAYS` | 14 дней молчания — уход |
| `WORST_TOPICS_LIMIT` | 10 худших тем в списке |
| `WORST_TOPIC_MIN_ANSWERS` | 5 ответов — порог попадания темы в худшие |
| `LOG_MAX_BYTES` | 8 МБ |
| `LOG_KEEP_FILES` | 4 |
| `LOG_TAIL_BYTES` | 512 КБ |
| `ADMIN_LOG_PAGE` | 200 |

## Где реализация разошлась с замыслом

Спека (`docs/superpowers/specs/2026-08-21-edukator-admin-design.md`) выполнена
целиком: предъявитель, оба замка, три слоя, журнал, CLI и экраны — те самые.
Разошлись подробности, и каждая — на найденной по дороге причине.

- **Курсор ленты аварий — не отметка времени, а `<отметка>#<сколько записей этой
  отметки уже отдано>`.** Спека называет `before=` временем. Аварии приходят
  пачкой, и несколько записей одной миллисекунды — обычное дело: строгое «раньше
  чем» теряло бы соседей по границе страницы, нестрогое повторяло бы их вечно.
  Плоскую отметку маршрут по-прежнему принимает — это тот же курсор с нулём.
  У журнала действий (`GET /api/admin/audit`) та же беда решена иначе: там есть
  `id`, и курсор идёт парой `(at, id)`.
- **Хвост читается с одним байтом сверх `LOG_TAIL_BYTES`.** Только по байту
  перед срезом видно, разрубил он строку или лёг ровно на её начало. Без него
  целая запись выбрасывалась бы всякий раз, когда граница совпала с переводом
  строки.
- **Неизвестное `event` в запросе — 400, а не пустая лента.** Пустой ответ на
  опечатку в фильтре читался бы как «аварий такого рода не было», то есть
  скрывал бы ровно то, ради чего в журнал и смотрят. Тем же правилом закрыт
  фильтр по действию в журнале действий.
- **Ленты аварий нет в `APP_PAGES`.** Спека добавляет туда `/admin` и
  `/admin/child/:childId`; лента живёт состоянием внутри `AdminApp`, потому что
  третий адрес пришлось бы заводить в списке, а забытый там адрес ломается 404
  статики ровно в тот день, когда ленту открывают. Карточка ребёнка адресом
  осталась: её ссылку хочется уметь отправить себе же.
- **Слой 1 отдаёт ещё и список семей.** Спека держит в нём только цифры, а
  список семей рисует `AdminHomeScreen`. Вторым запросом он показывал бы цифры и
  семьи на разные моменты времени, поэтому `families` собирается тем же обходом
  `children`, что и размеры баз, — по-прежнему не открывая ни одной детской базы.
- **Выбор соединения вынесен в `server/tenant-opener.ts`.** Спека меняет
  сигнатуру `TenantOpener.open` и оставляет сборку опенера в `buildServer`. В
  замыкании сервера обе ветки — «пишущая аренда и будильник прогрева» против
  «второй handle только для чтения» — не проверялись бы без поднятого сервера, а
  вторая из них и есть второй замок.
- **Счётчик отказов первого замка — процессный модуль, а не колонка в базе.**
  `resolveTenant` между запросами не помнит ничего, а число обязано дожить до
  записи `impersonation-end`: он получил необязательный `onReadOnly`, а счёт
  ведёт `server/admin/impersonation-refusals.ts`. Запись в `control.db` на каждый
  отказ означала бы, что закрытый маршрут умеет заставить нас писать.
- **`failAuth` отдаёт `code` рядом с текстом.** «Только просмотр», «доступ
  закрыт» и «запрос пришёл не со страницы» отвечают одним 403, и по статусу
  клиент их не различает — то есть работающий замок показывался бы экраном
  поломки. Текст остаётся человеку, код — коду.
- **`/api/auth/me` проверяет заход раньше собственных cookie оператора.**
  Оператор заходит со своей машины, где живы обе его сессии, и ветка «пришли обе
  cookie» вернула бы ему свою же семью — баннера не было бы ровно там, где он и
  нужен. По той же причине на админском адресе клиент `me` не спрашивает вовсе.
- **`redactTokenText` рядом с `redactTokenUrl`.** Спека обещает `<token>` в
  журнале, но существующая замена берёт адрес целиком (и потому умеет сохранить
  строку запроса), а в журнале токен лежит внутри фразы — на таком тексте она не
  срабатывает вовсе. Адрес (`route`) по-прежнему идёт через `redactTokenUrl`.
- **`FailureLog` передаётся обязательным аргументом.** Умолчание в `dataDir()`
  писало бы из каждого теста в `data/` рядом с кодом, а каталог данных знает не
  всякий модуль: `registerErrorHandler` и `WarmupDispatcher` берут журнал
  аргументом — забытая передача обязана падать на сборке, а не терять аварию.
- **`tenant-detached` и `tenant-open-failed` — разные события.** Спека называет
  подмену файла одним из видов отказа открытия. Различаются они тем, есть ли
  файл на месте после отказа `openSession`, и зовут к разным действиям, так что
  одно имя на обе прятало бы первую.
- **Три константы слоя 2 сверх списка спеки.** `CHURN_SILENCE_DAYS = 14`,
  `WORST_TOPICS_LIMIT = 10`, `WORST_TOPIC_MIN_ANSWERS = 5`: «ушедшая семья» и
  «худшие темы» без чисел не считаются, а числа без буквального теста
  подменяются молча. `ADMIN_AUDIT_PAGE = 200` заведена по тому же правилу —
  размер страницы тот же, что у ленты аварий.
- **Сроки полосы захода проверяются юнит-тестом, а не e2e.** Сервер e2e живёт на
  зафиксированных часах, а полоса считает минуты настоящими, поэтому остаток
  срока сценарий не смотрит: это дело `ImpersonationBanner.test.tsx`.

## Post-Completion

*Требует ручного вмешательства или внешних систем — без чекбоксов.*

**Ручная проверка:**

- завести оператора на боевой машине через `npm run admin -- create` и убедиться,
  что вход работает по https с настоящей cookie `__Host-`
- зайти имперсонацией в собственного ребёнка и убедиться, что баннер виден, а
  «ответить» отказывает
- убедиться, что после суток работы `logs/` не превысил 32 МБ

**Риски, названные, но не закрытые:**

- один пароль открывает данные всех семей сразу — второго фактора в этой работе
  нет
- имперсонация показывает содержание чужого ребёнка; сдержана только аудитом и
  15-минутным TTL
- открытый экран статистики читает все детские базы раз в пять минут
