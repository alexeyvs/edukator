# Edukator: генерация заданий через codex (этап 2 из 4)

## Overview

Второй этап реализации Edukator. Ядро из этапа 1 умеет выбирать тему и оценивать
ответ, но заданий у него пока нет. Этот этап их производит.

Строятся пять вещей: клиент codex, генератор батчей, валидатор, воркер тёплой
очереди и посевной банк. Задача этапа сформулирована жёстко: **ученик никогда не
видит спиннер и никогда не получает кривое задание**. Первое обеспечивает
опережающая генерация, второе — независимая перепроверка каждого задания.

Замеры этапа подготовки: батч из 5 заданий с подсказкой, разбором и шуткой —
23.6 секунды на `gpt-5.6-terra`. Один батч покрывает 5-10 минут решения, поэтому
воркер успевает пополнять очередь, пока ученик занимается.

Утверждённая спека: `docs/superpowers/specs/2026-08-07-edukator-design.md`.
Предыдущий этап: `docs/plans/20260807-edukator-core.md` (реализация завершена,
открыты только ручные release gates в его Post-Completion).

**Этапы 3-4 (не входят в этот план):** экран забега и триаж; отображение
прогноза, родительский дашборд, боссы тем, диктант.

## Context (from discovery)

- Ветка `edukator-core` содержит завершённый этап 1: 320 тестов, покрытие 96.75%.
- **Клиент codex уже написан** в `scripts/build-curriculum.ts` и работает:
  `runCodexCli`, `codexArgs`, `codexOutputSchema`, `writeCodexSchema`,
  `parseCodexAnswer`, `CodexUnavailableError`, `CodexRunner`, таймаут
  `CODEX_TIMEOUT_MS`, повторы `DEFAULT_ATTEMPTS`. Его нужно **вынести и
  переиспользовать, а не писать заново**.
- Карты тем собраны: математика 23 темы, русский 24, английский 22; у каждой
  темы есть `prompt_seed` и `answer_format`.
- Таблица `task_bank` создана в этапе 1 и ждёт наполнения; `attempts`, `runs` и
  `disputes` тоже готовы.
- Нормализатор `server/normalize.ts` сверяет ответ с `answer` и `accept[]` по
  `answer_format` — генератор обязан заполнять `accept[]` под него.
- codex CLI 0.146.0, рабочая модель `gpt-5.6-terra`, запасная `gpt-5.6-luna`.
- **Подводный камень:** без закрытого stdin codex висит бесконечно; в коде это
  выражено как `stdio[0] = 'ignore'` в `spawn`.
- Экзамен 25.08.2026 — 18 дней. Слабый предмет: русский язык.

## Development Approach

- **Testing approach**: TDD для чистой логики (сборка промпта, разбор ответа,
  сверка валидатора, политика очереди). Для процессов и I/O допустим обычный
  порядок с подстановкой процесса-заглушки, как это уже сделано в
  `tests/build-curriculum.test.ts`.
- **Ни один тест не вызывает настоящий codex.** Реальные вызовы идут только в
  задаче наполнения посевного банка, вручную, и их результат коммитится.
- Каждая задача доводится до конца перед переходом к следующей.
- **КРИТИЧНО: каждая задача обязана содержать новые или обновлённые тесты**
  для кода, изменённого в этой задаче.
  - тесты не опциональны — это обязательная часть чеклиста
  - юнит-тесты на новые и на изменённые функции
  - новые тест-кейсы на новые ветви кода
  - тесты покрывают и успешные, и ошибочные сценарии
- **КРИТИЧНО: все тесты обязаны проходить до начала следующей задачи.**
- **КРИТИЧНО: этот файл плана обновляется при изменении объёма работ по ходу.**

## Testing Strategy

- **Unit tests**: обязательны для каждой задачи.
- **E2E tests**: интерфейса на этом этапе нет; появятся на этапе 3.
- **Тесты воркера** проверяют политику (пороги пополнения, предел
  одновременных процессов, откат на посевной банк) на подставном генераторе,
  без запуска процессов.
- **Содержательное качество заданий** автотестами не проверяется — его
  проверяет человек по выборке из посевного банка (Post-Completion).

## Progress Tracking

- Выполненные пункты помечаются `[x]` сразу по завершении.
- Новые обнаруженные задачи добавляются с префиксом ➕.
- Проблемы и блокеры документируются с префиксом ⚠️.
- План держится в синхроне с фактически сделанной работой.

## What Goes Where

- **Implementation Steps** (`[ ]`): всё, что достижимо внутри репозитория.
- **Post-Completion** (без чекбоксов): выборочная вычитка сгенерированных
  заданий человеком, заполнение профиля ученика.

## Implementation Steps

### Task 1: Вынести клиент codex в переиспользуемый модуль

- [x] создать `server/codex/client.ts` и перенести туда из
      `scripts/build-curriculum.ts`: `CodexRequest`, `CodexRunner`,
      `CodexUnavailableError`, `codexArgs`, `codexOutputSchema`,
      `writeCodexSchema`, `runCodexCli`, `parseCodexAnswer`, константы модели,
      таймаута и числа попыток
- [x] оставить в клиенте только общее: ничего специфичного для карты тем
- [x] добавить выбор модели параметром запроса с умолчанием `gpt-5.6-terra` и
      запасной `gpt-5.6-luna`
- [x] переключить `scripts/build-curriculum.ts` на новый модуль, удалив
      перенесённый код (не дублировать)
- [x] перенести относящиеся к клиенту тесты в `tests/codex-client.test.ts`
- [x] написать тест: выбор запасной модели при явном указании
- [x] запустить тесты — сборка карт тем обязана продолжать работать

### Task 2: Схема задания и её проверка

- [x] создать `schemas/tasks.json` — JSON Schema батча: `items[]` с полями
      `question`, `answer`, `accept[]`, `hint`, `explain`, `joke`, `difficulty`
- [x] создать `server/codex/task-schema.ts`: разбор ответа codex в задания с
      проверкой инвариантов, не выразимых схемой
- [x] проверять, что `answer` входит в `accept[]`, `accept[]` непуст и без
      дублей, `difficulty` в 1..3, `hint` не содержит текста `answer`
- [x] проверять согласованность с `answer_format` темы: для `number` каждый
      элемент `accept[]` обязан распознаваться нормализатором как число
- [x] написать тест: корректный батч разбирается
- [x] написать тест: ошибочные сценарии — `answer` вне `accept[]`, пустой
      `accept[]`, дубли, `difficulty` вне диапазона, подсказка с ответом,
      нечисловой `accept[]` при `answer_format: number`
- [x] запустить тесты — обязаны пройти до задачи 3
- ➕ вынесена общая обвязка Ajv в `server/json-schema.ts`: настройка и текст
      ошибок были в `server/curriculum.ts`, копировать их в разбор батча нельзя

### Task 3: Сборка промпта генератора

- [x] создать `server/codex/prompt.ts` со сборкой промпта из пяти частей:
      персона напарника, `prompt_seed` темы, профиль ученика, целевая
      сложность, последние формулировки с указанием не повторяться
- [x] вынести персону в `content/persona.md` — саркастичный напарник, тон
      старшего брата, без сюсюканья и без похвалы за тривиальное
- [x] задать нейтральное умолчание интересов в профиле, чтобы промпт работал
      до того, как профиль заполнен
- [x] экранировать содержимое профиля: интересы вводит человек, и они не должны
      уметь переопределять инструкции промпта
- [x] написать тест: промпт содержит персону, seed темы, интересы и запрет на
      повтор переданных формулировок
- [x] написать тест: промпт собирается на пустом профиле
- [x] написать тест: попытка инъекции из поля интересов не ломает структуру
- [x] запустить тесты — обязаны пройти до задачи 4
- ➕ прошлые формулировки уходят в промпт тем же блоком JSON, что и профиль:
      они приходят из ответов модели и переопределять инструкции тоже не должны
- ➕ `TASK_BATCH_SIZE` и `RECENT_LIMIT` живут в `prompt.ts`: размер батча задаёт
      промпт, генератору задачи 4 остаётся его переиспользовать

### Task 4: Генератор батча заданий

- [ ] создать `server/codex/generate.ts`: тема и целевая сложность на входе,
      разобранный батч на выходе
- [ ] запрашивать батч из 5 заданий одним вызовом (замер: 23.6 с на батч)
- [ ] повторять вызов при непрохождении разбора, дописывая текст ошибки в
      промпт повторной попытки, не более трёх попыток
- [ ] отдавать `CodexUnavailableError` наверх, а не глотать: воркер обязан
      узнать о недоступности codex
- [ ] написать тест: успешная генерация на подставном исполнителе
- [ ] написать тест: ошибочные сценарии — невалидный JSON, проваленные
      инварианты, исчерпание попыток, недоступность codex
- [ ] написать тест: текст ошибки попадает в промпт повторной попытки
- [ ] запустить тесты — обязаны пройти до задачи 5

### Task 5: Валидатор заданий

- [ ] создать `server/codex/validate.ts`: второй вызов codex, получающий
      **только условия, без ответов**
- [ ] требовать от валидатора решить каждое задание и отдельно оценить:
      условие однозначно, ситуация не натянута, тема соответствует заявленной,
      содержание уместно для подростка
- [ ] сверять ответы валидатора с ответами генератора через нормализатор из
      `server/normalize.ts`, а не строковым сравнением
- [ ] отбраковывать задание при расхождении ответов или любой проваленной
      оценке, сохраняя причину отбраковки для диагностики
- [ ] написать тест: совпадающие ответы проходят, расходящиеся отбраковываются
- [ ] написать тест: сверка идёт через нормализатор — `−45` и `-45 монет`
      признаются совпадением
- [ ] написать тест: провал любой из четырёх оценок отбраковывает задание
- [ ] написать тест: ошибочные сценарии — валидатор вернул меньше вердиктов,
      чем заданий; недоступность codex
- [ ] запустить тесты — обязаны пройти до задачи 6

### Task 6: Хранение банка заданий и защита от повторов

- [ ] создать `server/codex/bank.ts`: запись провалидированных заданий в
      `task_bank`, выдача задания по теме, пометка выданного
- [ ] хранить нормализованный отпечаток формулировки и отвергать дубли внутри
      темы на входе в банк
- [ ] отдавать последние N формулировок темы для промпта из задачи 3
- [ ] считать остаток непросмотренных заданий по теме — это вход для воркера
- [ ] написать тест: задание пишется и выдаётся, повторная выдача исключена
- [ ] написать тест: дубль формулировки не попадает в банк
- [ ] написать тест: ошибочные сценарии — пустой банк по теме, тема вне карты
- [ ] запустить тесты — обязаны пройти до задачи 7

### Task 7: Воркер тёплой очереди

- [ ] создать `server/codex/worker.ts`: фоновое пополнение банка по темам,
      которые планировщик выберет ближайшими
- [ ] держать не менее 8 провалидированных заданий на активную тему, запускать
      долив при падении ниже 4
- [ ] ограничить одновременные вызовы codex двумя
- [ ] брать список активных тем у планировщика этапа 1, а не хранить свой
- [ ] при недоступности codex прекращать попытки с возрастающей паузой и
      писать в лог, не роняя сервер
- [ ] написать тест: долив запускается на пороге 4 и останавливается на 8
- [ ] написать тест: одновременных вызовов не больше двух
- [ ] написать тест: недоступность codex не роняет воркер и логируется
- [ ] написать тест: воркер не генерирует по темам вне выборки планировщика
- [ ] запустить тесты — обязаны пройти до задачи 8

### Task 8: Посевной банк и опережающее наполнение

- [ ] создать `scripts/prefetch.ts` и скрипт `npm run prefetch`: наполнение
      банка по ближайшим темам, с ограничением числа батчей
- [ ] сгенерировать посевной банк — не менее 30 провалидированных заданий на
      предмет — и закоммитить в `content/seed-bank/{math,russian,english}.json`
- [ ] реализовать загрузку посевного банка в `task_bank` при первом запуске
- [ ] реализовать откат: при пустой очереди и недоступном codex задание берётся
      из посевного банка
- [ ] написать тест: посевной банк загружается и проходит проверки задачи 2
- [ ] написать тест: при недоступном codex и пустой очереди задание всё равно
      выдаётся
- [ ] написать тест: посевной банк не загружается повторно при перезапуске
- [ ] запустить тесты — обязаны пройти до задачи 9

### Task 9: Маршруты выдачи задания и разбора спорного ответа

- [ ] создать `server/routes/session.ts`: `GET /api/session/next` — следующее
      задание по выбору планировщика, `POST /api/session/answer` — проверка
      ответа, обновление модели знаний, запись попытки
- [ ] реализовать `POST /api/session/dispute` — постановка спорного ответа в
      очередь фоновой перепроверки
- [ ] реализовать фоновый разбор спора: при подтверждении правоты ученика
      пересчитать модель знаний и дополнить `accept[]` задания
- [ ] отдавать задание без полей `answer` и `accept[]` — ответ не должен
      приезжать в браузер вместе с условием
- [ ] написать тест: выдача задания не содержит ответа
- [ ] написать тест: верный и неверный ответ по-разному двигают модель знаний
- [ ] написать тест: спор подтверждён — баллы возвращаются, `accept[]` растёт
- [ ] написать тест: ошибочные сценарии — задания нет, ответ на чужое задание,
      повторный ответ на то же задание
- [ ] запустить тесты — обязаны пройти до задачи 10

### Task 10: Verify acceptance criteria

- [ ] проверить, что все требования из Overview реализованы
- [ ] проверить, что ни один тест не вызывает настоящий codex
- [ ] прогнать полный набор юнит-тестов
- [ ] прогнать `typecheck` — ошибок быть не должно
- [ ] прогнать линтер — все замечания исправить
- [ ] проверить покрытие новых модулей (не ниже 80%)
- [ ] убедиться, что посевной банк проходит проверки задачи 2 целиком

### Task 11: [Final] Обновление документации

- [ ] описать в README генерацию: `npm run prefetch`, посевной банк, поведение
      при недоступном codex
- [ ] задокументировать формат задания и требования к `accept[]`
- [ ] зафиксировать обнаруженные при реализации закономерности

## Technical Details

**Поток генерации:**

```
планировщик → активные темы → воркер → генератор (codex, батч 5)
                                     → валидатор (codex, без ответов)
                                     → сверка через normalize.ts
                                     → task_bank
```

**Формат задания в `task_bank`:** `topic_id`, `question`, `answer`,
`accept` (JSON), `hint`, `explain`, `joke`, `difficulty`, статус, отпечаток
формулировки.

**Вызов codex:** `--ephemeral --skip-git-repo-check --output-schema
schemas/tasks.json -o <файл>`, stdin закрыт, модель `gpt-5.6-terra`.

**Пороги воркера:** пополнять ниже 4 заданий на активную тему, останавливаться
на 8, не более двух одновременных вызовов codex.

## Post-Completion

*Требует ручного вмешательства, чекбоксов нет*

| Release gate | Статус | Владелец | Срок | Свидетельство |
|---|---|---|---|---|
| Вычитка выборки заданий | pending | не назначен | до этапа 3 | — |
| Заполнение профиля ученика | pending | не назначен | до этапа 3 | — |

**Вычитка выборки (обязательна перед этапом 3):**

- Прочитать по 10 заданий каждого предмета из посевного банка и оценить: условия
  осмысленны, юмор попадает в возраст, разборы понятны. Валидатор ловит
  расхождение ответов и натянутые ситуации, но вкус проверить не может.
- Если тон напарника промахивается — править `content/persona.md`, это одно
  место на всё приложение.

**Заполнение профиля:**

- Указать имя ребёнка и интересы (игры, стримеры, увлечения). До этого генерация
  работает на нейтральном умолчании и сюжеты будут пресными — это единственное,
  что реально отличает «задачи из его мира» от обычного задачника.

**Следующие этапы:**

- Этап 3 — экран забега и триаж; после него можно начинать заниматься.
- Этап 4 — отображение прогноза, родительский дашборд, боссы тем, диктант.
