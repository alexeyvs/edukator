# Edukator

Домашнее приложение для подготовки ребёнка по назначенным учебным курсам.
Оператор создаёт курсы с произвольными названием и классом, публикует
версионированные карты тем и при необходимости загружает PDF-источники;
родитель назначает курсы ребёнку и может исключить отдельные темы. Бюджет
подготовки ограничен, поэтому приложение ищет пробелы и тратит время ученика
прежде всего на них.

Проектная спека: [`docs/superpowers/specs/2026-08-07-edukator-design.md`](docs/superpowers/specs/2026-08-07-edukator-design.md).
Планы этапов: [`docs/plans/20260807-edukator-core.md`](docs/plans/20260807-edukator-core.md) (этап 1),
[`docs/plans/20260807-edukator-generation.md`](docs/plans/20260807-edukator-generation.md) (этап 2),
[`docs/plans/completed/20260808-edukator-run-triage.md`](docs/plans/completed/20260808-edukator-run-triage.md) (этап 3),
[`docs/plans/completed/20260808-edukator-game-parents.md`](docs/plans/completed/20260808-edukator-game-parents.md) (этап 4),
[`docs/plans/completed/20260809-background-learning-materials.md`](docs/plans/completed/20260809-background-learning-materials.md) (персональные разборы),
[`docs/plans/completed/20260813-required-personal-review-gate.md`](docs/plans/completed/20260813-required-personal-review-gate.md) (обязательный первый разбор),
[`docs/plans/20260819-edukator-multi-tenancy.md`](docs/plans/20260819-edukator-multi-tenancy.md) (многоарендность),
[`docs/plans/20260821-edukator-admin.md`](docs/plans/20260821-edukator-admin.md) (админка оператора),
[`docs/superpowers/specs/2026-08-22-configurable-courses-design.md`](docs/superpowers/specs/2026-08-22-configurable-courses-design.md) и
[`docs/plans/20260822-configurable-courses.md`](docs/plans/20260822-configurable-courses.md) (настраиваемые курсы).

## Что уже есть

Готов **этап 1 — ядро и данные**: всё, что работает без LLM и без интерфейса.

| Модуль | Назначение |
|---|---|
| `server/db.ts` | SQLite (`better-sqlite3`), схема и идемпотентные миграции, профиль |
| `server/curriculum.ts` | типы и валидация графа тем; файловые карты используются только для legacy-bootstrap |
| `server/normalize.ts` | сверка ответа ученика с эталоном по `answer_format` (`number` / `text` / `choice`) |
| `server/mastery.ts` | модель знаний: `mastery`, `confidence`, пробелы, интервальное повторение |
| `server/scheduler.ts` | приоритет тем и план забегов на день |
| `server/forecast.ts` | прогноз оценки, полоса погрешности, снимки и тренд |
| `scripts/extract-toc.ts` | оглавление учебника из PDF → `content/raw-toc/<subject>.txt` |
| `scripts/toc.ts` | поиск страниц оглавления по плотности пунктов; чистый модуль без ввода-вывода, тестируется на синтетике |
| `server/ocr-runner.ts`, `server/catalog-worker.ts` | перезапускаемый Linux OCR через OCRmyPDF/Tesseract/Poppler/qpdf и persistent jobs |
| `server/course-catalog.ts`, `server/curriculum-provider.ts` | каталог редакций и персональные неизменяемые снимки программы |

Готов **этап 2 — генерация заданий**: банк заданий наполняется codex заранее,
чтобы ученик не ждал модель.

| Модуль | Назначение |
|---|---|
| `server/codex/client.ts` | общий клиент codex: аргументы, запуск, схема для `--output-schema`, разбор ответа |
| `server/codex/prompt.ts` | промпты генератора, проверяющего и разбора спора; персона из `content/persona.md` |
| `server/codex/task-schema.ts` | разбор батча заданий и инварианты, не выразимые схемой |
| `server/codex/generate.ts` | батч из 5 заданий одним вызовом, до трёх попыток с текстом ошибки в промпте |
| `server/codex/validate.ts` | независимая перепроверка: второй вызов codex видит только условия |
| `server/codex/bank.ts` | `task_bank`: запись, выдача, отпечаток формулировки, остаток по теме |
| `server/codex/worker.ts` | фоновое пополнение очереди по активным темам с отступом при недоступном codex |
| `server/codex/seed-bank.ts` | посевной банк `content/seed-bank/*.json`: загрузка, откат, выгрузка |
| `server/codex/dispute.ts` | разбор спорного ответа моделью |
| `server/codex/integrity.ts`, `schemas/integrity-review.json` | независимая проверка осмысленности ответов завершённого занятия |
| `server/integrity.ts`, `server/routes/integrity.ts` | сохранённое состояние проверки, повтор сомнительного ответа и HTTP-контракт |
| `server/session.ts`, `server/routes/session.ts` | занятие: выдача задания, приём ответа, спор |
| `scripts/prefetch.ts` | ручное опережающее наполнение банка и сборка посева |

Готов **этап 3 — забег и триаж**: ученик занимается через React-интерфейс,
проходит первичную диагностику и обычные забеги по 12 заданий.

| Модуль | Назначение |
|---|---|
| `server/run.ts` | жизненный цикл забега, прогресс, закрытие, XP и снимок прогноза |
| `server/triage.ts` | диагностическая выдача по разным темам с адаптивной сложностью 1–3 |
| `server/xp.ts` | единая формула XP за ответ |
| `server/routes/{run,triage,profile}.ts` | HTTP-контракты плана дня, забега, триажа и профиля |
| `server/codex/concurrency.ts` | два слота codex для воркера и отдельный слот для споров |
| `web/` | Vite, React 19 и Tailwind: знакомство, главный экран, забег, финал, триаж и профиль |
| `e2e/` | браузерские сценарии забега, спора, триажа, босса и `/parents` без обращения к настоящему codex |

Готов **этап 4 — игровой слой и родительский дашборд**: серия занятий и карта
тем помогают видеть прогресс, боссы окончательно закрывают освоенные темы, а
прямой адрес `/parents` показывает прозрачную недельную сводку — теперь под
входом, но по-прежнему видимую и самому ученику.

| Модуль | Назначение |
|---|---|
| `server/streak.ts` | текущая и лучшая серия по завершённым обычным забегам и московским календарным дням |
| `server/boss.ts`, `server/boss-loss.ts` | жизненный цикл боя, ответы, споры, победа и поражение |
| `server/boss-prep.ts` | фоновая подготовка пяти свежих независимо проверенных заданий |
| `server/parents.ts` | единый read-only снимок прогнозов, активного времени, пробелов, ленты и флагов за семь суток |
| `server/routes/{boss,parents}.ts` | HTTP-контракты игрового слоя и родительской сводки |
| `web/src/{BossScreen,ParentsScreen}.tsx` | бой без подсказок и дашборд по прямому адресу `/parents` |

Готовы **персональные разборы слабых тем**: после триажа фоновый воркер заранее
собирает безопасную теорию и тест из пяти независимо проверенных заданий.

| Модуль | Назначение |
|---|---|
| `server/learning.ts` | атомарный жизненный цикл материала и последовательных `lesson`-забегов до зачёта |
| `server/learning-prep.ts` | отбор пробелов, генерация, проверка, публикация и retirement |
| `server/codex/learning-material*.ts` | структурированный формат теории, промпты и независимая проверка |
| `server/routes/learning.ts` | HTTP-контракт чтения, открытия, старта и завершения теста |
| `web/src/{LearningScreen,LearningFinishScreen}.tsx` | чтение безопасных блоков, переход к тесту и результат зачёта |
| `e2e/` | сценарии забега, спора, триажа, персонального разбора, босса и `/parents` без настоящего codex |

Готова **базовая многоарендность**: родитель заводится по email и паролю
закрытым списком, ведёт нескольких детей, и каждый ребёнок занимается **в своей
отдельной базе**, входя со своего компьютера по одноразовой ссылке.

| Модуль | Назначение |
|---|---|
| `server/control-db.ts` | управляющая база: родители, приглашения, сессии, дети, устройства, суточная квота codex и счётчики неудачных входов |
| `server/secrets.ts`, `server/parent-pin.ts` | `scrypt` с индивидуальной солью для пароля и PIN, серверный pepper PIN |
| `server/auth.ts` | предъявители семьи, проверка принадлежности ребёнка и защита изменяющих запросов от чужого источника |
| `server/tenant-registry.ts` | ленивый реестр детских баз с миграцией, посевом и своими координаторами споров и integrity-review |
| `server/data-dir.ts`, `server/data-lock.ts` | каталог данных `control.db` + `children/<id>.db` и замок «одно живое приложение на каталог» |
| `server/codex/dispatcher.ts`, `server/codex/quota.ts` | честная очередь фоновой генерации между детьми и 60 вызовов модели на ребёнка в сутки |
| `server/routes/{auth,family}.ts` | вход, приглашения, семья, устройства и PIN |
| `web/src/{LoginScreen,InviteScreen,JoinScreen,FamilyScreen}.tsx` | вход, установка пароля по ссылке, погашение детской ссылки и экран семьи |
| `scripts/{parent,adopt-single-user,backup}.ts` | родители из командной строки, разовый перенос однопользовательской базы и копия каталога данных |

Готова **админка оператора**: приложение живёт на VPS и держит чужие семьи,
поэтому у оператора есть свой вход, свои отчёты и заход в чужую семью **только
на просмотр** — вместо ssh и `sqlite3` руками.

| Модуль | Назначение |
|---|---|
| `server/auth.ts`, `server/routes/tenant-context.ts` | четвёртый предъявитель `admin` и отдельное разрешение админских маршрутов без аренды |
| `server/admin/{overview,stats,child-detail}.ts` | три слоя статистики: `control.db`, обход всех детских баз с кешем и карточка одного ребёнка |
| `server/admin/child-readonly.ts` | соединение отчётов: `readonly`, без миграции, мимо реестра арендаторов |
| `server/admin/impersonation-tenants.ts`, `server/tenant-opener.ts` | второй замок захода: своё соединение с `PRAGMA query_only = ON` |
| `server/log.ts` | хранилище аварий `logs/app.jsonl` с ротацией по размеру и чтением хвоста |
| `server/routes/admin/*.ts` | вход оператора, сводка, статистика, карточка ребёнка, лента аварий, заход и журнал действий |
| `web/src/admin/*`, `web/src/app-route.ts` | экраны админки, полоса захода и чистый выбор состояния клиента |
| `scripts/admin.ts` | операторы из командной строки: завести, сменить пароль, отключить |

Готов **настраиваемый каталог курсов**: список предметов и класс больше не
зашиты в приложение. `subject` в старых строках БД и API сохранён как
совместимое имя идентификатора курса (`CourseId`); отображаемые `title` и
`grade` ключами не являются. `overall` зарезервирован для общего прогноза.

| Компонент | Назначение |
|---|---|
| `server/course-catalog.ts` | курс, единственный редактируемый draft, стабильные ID тем, проверка графа, атомарная публикация и архивирование |
| `server/course-assignments.ts` | назначения ребёнку и отрицательный список исключённых тем; новая тема опубликованной редакции включается автоматически |
| `server/curriculum-provider.ts` | персональный immutable `CurriculumSnapshot`; учебное ядро не читает `control.db` напрямую |
| `server/course-artifacts.ts` | потоковая безопасная загрузка PDF в `catalog/artifacts`, лимиты, SHA-256, qpdf и атомарная запись |
| `server/catalog-worker.ts` | одна OCR-задача одновременно, page checkpoints, восстановление очереди после рестарта и retry диапазона |
| `server/course-retrieval.ts`, `server/course-drafting.ts` | FTS retrieval по страницам и асинхронная сборка draft по OCR с ограниченным source context |
| `web/src/admin/AdminCoursesScreen.tsx`, `web/src/admin/AdminCourseEditor.tsx` | каталог, редакции, редактор тем, upload/OCR/drafting/publish/archive |
| `web/src/FamilyScreen.tsx` | назначение опубликованных курсов и исключение отдельных тем |

Опубликованная редакция и её артефакты неизменяемы. Новый publish применяется
к следующей операции всех назначенных детей; уже начатый run хранит
`course_revision_id` и завершается на прежней редакции. Снятие назначения не
удаляет прогресс и не обрывает активный run.

В **этап 5** явно перенесены диктант, ElevenLabs/TTS и пробный экзамен. Они не
входят в текущий игровой слой и не запускаются существующим сервером.

## Установка

Для сервера и тестов нужен Node 26 или новее.

```sh
npm install
```

Фоновой генерации заданий, персональных материалов и сборке draft нужны Codex
CLI и OCR-зависимости; без Codex основное обучение продолжает работать на уже
подготовленном банке и опубликованных материалах:

- **`codex` в `PATH`** с доступом к модели `gpt-5.6-sol` (запасная —
  `gpt-5.6-terra`, см. [Модель по ролям](#модель-по-ролям)) — конвейер проверен
  с codex CLI 0.146.0 (`codex --version`); у других версий может отличаться
  поддержка структурированного вывода. Иначе
  legacy-команда `npm run build-curriculum` падает сразу, не тратя попыток
  (`codex не найден: ожидался исполняемый файл «codex» в PATH`), а
  `npm run prefetch` не пополняет банк и занятие живёт на посевном банке;
  подготовка боссов, персональных материалов (теории и теста) и разбор споров
  тоже требуют доступного Codex CLI. Без него остальное приложение продолжает
  работать, но карточки «Разобрать слабое место» не появятся
  (см. [Генерация заданий](#генерация-заданий));
- **Ubuntu 22.04+ с OCRmyPDF 13+, Tesseract 5+ и языками `rus`, `eng`,
  Poppler 22+ и qpdf 10+** — production OCR загруженных PDF. Идемпотентная
  установка: `sudo ./scripts/install-ocr-dependencies.sh`; read-only проверка:
  `./scripts/install-ocr-dependencies.sh --check`. Старый macOS Vision нужен
  только legacy-команде `npm run extract-toc` и не участвует в catalog worker.

`tsx`, `pdf-parse`, React и раздача статики — рабочие зависимости, так что
сервер и конвейер (`extract-toc`, `build-curriculum`, `prefetch`) поднимаются и после
`npm install --omit=dev`, если `web/dist` уже собран. Для сборки интерфейса
нужен dev-набор. Без него не работают проверки и сборка:
`test`, `test:e2e`, `coverage`, `typecheck`, `lint` и `build:web`.

npm 11 блокирует install-скрипты зависимостей. Разрешения для `better-sqlite3`
(нативная сборка) и `esbuild` (бинарник) уже зафиксированы в `allowScripts` в
`package.json` — без них не поднимется ни база, ни vitest. Если npm всё же
спросит, ответ даёт `npm approve-scripts`.

## Запуск

```sh
npm run dev         # сервер с watch на :3000 и Vite на :5173
npm run build:web   # production-сборка интерфейса в web/dist
npm start           # сервер и собранный интерфейс на :3000
./run.sh             # собрать интерфейс и запустить сервер с Family Safety-контроллером
```

### Первый запуск: родитель, ребёнок, устройство

Самостоятельной регистрации нет намеренно: родители заводятся закрытым списком
из командной строки, и оттуда же чинится забытый пароль — почтового
восстановления тоже нет.

```sh
export EDUKATOR_PIN_PEPPER="$(openssl rand -base64 24)"   # от 16 знаков, в базе не лежит

npm run parent -- create --email родитель@example.com     # завести запись
npm run parent -- invite --email родитель@example.com     # печатает /invite/<token>
npm run parent -- pin    --email родитель@example.com     # PIN спрашивается со stdin
npm run parent -- password --email родитель@example.com   # смена пароля, гасит все сессии
npm run parent -- disable  --email родитель@example.com   # отключить запись целиком
```

Пароль — от 10 до 128 знаков (верхний предел стоит **до** KDF: длинный пароль
иначе становится способом занять процессор), PIN — от 6 до 12 цифр.

`disable` гасит сессии родителя, его детей и все их устройства разом; повторное
отключение уже отключённого — отказ, а не молчаливый no-op. Он же прекращает
фоновую генерацию для всей семьи: ни ночной `npm run prefetch -- --all`, ни
`--child` отключённого родителя больше не тратят вызовы модели. У всех трёх скриптов
(`parent`, `backup`, `adopt`) есть `--data-dir`; `--password` и `--pin` они
отвергают намеренно и говорят почему.

Ссылка `/invite/<token>` живёт неделю и передаётся родителю руками. Открыв её,
он ставит пароль и получает сессию; дальше вход обычный — email и пароль. Ни
пароль, ни PIN не принимаются флагами: аргументы командной строки видны в `ps`
любому пользователю машины и остаются в истории оболочки, поэтому скрипт всегда
спрашивает секрет сам и в вывод его не печатает.

Дальше всё делается на экране «Семья»: родитель заводит ребёнка, выпускает для
его компьютера одноразовую ссылку `/join/<token>` (живёт сутки) и открывает её
на детской машине. Ссылка гасится самим открытием — лишний экран «нажмите,
чтобы продолжить» ничего не проверял бы, — поэтому открывать её надо там, где
она и нужна. После погашения устройство получает cookie на десять лет, и
настоящий выключатель — серверный отзыв устройства. PIN родителя тоже меняется
на этом экране, если pepper настроен.

Контроллеру доступа выпускается устройство вида «агент». Экран «Семья»
показывает для него **ту же ссылку** `/join/<token>`: постоянный токен рождается
только при её погашении и виден ровно один раз — на странице, которая её
погасила. Его и вписывают в конфигурацию контроллера.

Родитель сохраняется в сессии 30 дней без единого обращения и в любом случае не
дольше 90 дней с момента входа. Смена **пароля** гасит все прежние родительские
сессии и все токены детских устройств, а вместе с ними — **ещё не погашенные**
ссылки, и родительскую `/invite/<token>`, и детскую `/join/<token>`, выпущенные
до неё: право поставить пароль по ссылке равно полному входу, и утёкшая ссылка
иначе пережила бы единственное средство, которое от неё советуют. Ссылки
придётся выпустить заново, и `npm run parent -- password` говорит об этом
вслух. Смена PIN ничего
не гасит намеренно: PIN — это подтверждение родителя за детской машиной, а не
ключ от учётной записи, и его смена не значит, что учётную запись увели.

Уже работающую однопользовательскую базу переносить руками не надо:

```sh
npm run adopt -- --email родитель@example.com --name Тимофей
```

Перенос берёт замок каталога данных и при живом сервере отказывается: пока
ребёнок заводится, его база перебирается из времянки в рабочее место, и сервер
рядом обслуживал бы то, чего ещё нет. Остановите `npm start` перед переносом.

Скрипт заводит родителя (или находит по адресу), заводит ребёнка и кладёт на
место его базы **копию** нынешней `edukator.db` через `VACUUM INTO`. Копию, а не
перенос файла: оригинал остаётся откатом, пока перенесённая база не проверена
живым занятием. Оборванный перенос продолжается повторным запуском с любого
места — в том числе после обрыва между переносом файла и пометкой «готов», когда
база уже лежит на рабочем месте: копировать её заново незачем, повтор лишь
догоняет схему и ставит пометку. Отказ стоит только на уже готовом ребёнке —
иначе повторный запуск затёр бы его вчерашним снимком. Путь исходной базы берётся из `--from` или прежней переменной
`EDUKATOR_DB`; больше о ней не знает никто, кроме этого скрипта.

### Оператор: админка, заход в семью и логи

Приложение стоит на VPS и держит чужие семьи, поэтому у оператора есть свой
вход. Самостоятельной регистрации у него нет тем более, чем у родителя:
приглашений по ссылке и почтового восстановления не существует вовсе, а завести
оператора и починить ему пароль можно только с самой машины, где лежит
`control.db`.

```sh
npm run admin -- create   --email оператор@example.com   # завести запись
npm run admin -- password --email оператор@example.com   # пароль спрашивается со stdin
npm run admin -- disable  --email оператор@example.com   # отключить и погасить сессии
```

Пароль оператора — от **16** знаков: он открывает данные всех семей сразу.
`create` заводит управляющую базу, если её ещё нет; остальные команды идут с
`fileMustExist`, чтобы опечатка в `--data-dir` не заводила пустую базу рядом.

Вход — на `/admin`, тем же email и паролем. Cookie оператора отдельная
(`__Host-edu_admin`), сессия живёт 30 минут без обращений и в любом случае не
дольше 8 часов; смена пароля и отключение гасят её немедленно. Админские
маршруты закрыты родителю, ребёнку и агенту наглухо, а обычные семейные —
оператору: это разные предъявители, а не разные права одного.

Админка показывает три слоя. Сводка (`GET /api/admin/overview`) читает только
`control.db` и не открывает ни одной детской базы — этот экран обязан
открываться ровно тогда, когда с базами что-то не так. Статистика
(`GET /api/admin/stats`) обходит все детские базы `readonly`, поэтому её ответ
кешируется на 5 минут и показывается с отметкой времени; пересчёт — кнопкой
(`?refresh=1`). Карточка ребёнка (`/admin/child/:childId`,
`GET /api/admin/children/:childId`) кеша не имеет: по
жалобе смотрят состояние сейчас. Содержания не читает ни один слой — ни текстов
ответов, ни формулировок споров, ни теории.

Прочитать написанное ребёнком можно только **заходом** в семью
(`POST /api/admin/impersonate`): оператор смотрит детский или родительский экран
так, как его видит семья, с несъёмной полосой поверх. Заход живёт 15 минут,
держится двумя независимыми замками на запись — отказ 403 «Только просмотр» и
`PRAGMA query_only = ON` на отдельном соединении (вместе с координаторами спора
и проверки осмысленности: они носят своё, пишущее, и подменяются
отказывающими), — не двигает отметки активности ребёнка и не будит фоновую
генерацию. Выход из самой админки заход тоже закрывает: переживший его заход
выигрывал бы у собственных cookie оператора, а снять его было бы уже нечем. Начало и конец захода, вход и
отказ входа попадают в `admin_audit` (`GET /api/admin/audit`); в записи о конце
стоит счётчик отказанных попыток записи.

Аварии пишутся не только в journald, но и файлом:

```
data/logs/app.jsonl      # текущий; на границе 8 МБ уезжает в app.1.jsonl
data/logs/app.1.jsonl    # …и дальше до app.3.jsonl
```

Четыре файла по 8 МБ — это весь ретеншен: удаления по времени нет, зато журнал
не может съесть место под детские базы. Каждая строка — JSON с отметкой
времени, закрытым `event` (`server-error`, `tenant-open-failed`,
`tenant-detached`, `control-error`, `startup-failed`, `codex-unavailable`,
`codex-run-failed`, `sweep-failed`, `prefetch-failed`, `backup-failed`,
`login-lockout`), сообщением и, если есть, ребёнком, маршрутом и статусом. Тел
запросов и ответов ребёнка в журнале нет, а токен из адреса заменяется на
`<token>`. Лента в админке (`GET /api/admin/logs`) читает **хвост** — последние
512 КБ, при нужде предыдущий файл, — и отдаёт не больше 200 записей за страницу
с фильтрами по событию и ребёнку.

## Блокировка Windows до выполнения плана

Автоматический расчёт `GET /api/gate/status` открывает доступ после выполнения
двух независимых условий: за московский календарный день завершены три обычных
забега и зачтён обязательный персональный разбор. Активная родительская команда
может временно переопределить этот результат. Триаж, босс и персональный тест не входят в
число трёх забегов. Обязательным выбирается первый опубликованный материал по
`ready_at`, затем `id`; остальные материалы добровольны. Публикация после
третьего забега не переблокирует уже полученный доступ, а переносит именно этот
материал на следующие московские сутки, даже если до полуночи он уже зачтён или
автоматически снят.

Неофициальный контроллер Microsoft Family Safety устанавливается отдельно и
не запускается обычной командой `npm start`:

```sh
# Нужен Python 3.11+. При необходимости: EDUKATOR_PYTHON=/путь/python3.12
npm run family:setup
npm run family:login

# Серверный pepper PIN (от 16 знаков) нужен и серверу, и `npm run parent -- pin`.
# Открытый PIN нигде не хранится: в базе лежит только scrypt-хеш, а pepper
# добирает стойкость, которой у шестизначного значения нет, и в базу не попадает.
export EDUKATOR_PIN_PEPPER="$(openssl rand -base64 24)"

# После production-сборки запускает сервер на всех домашних интерфейсах
# и контроллер одним комплектом
npm run start:family
```

`npm run family:login` спрашивает ещё адрес сервера и **агентский токен**. Его
родитель заводит на экране «Семья» устройством вида `agent` и получает по той же
одноразовой ссылке `/join/<token>`, что и обычное устройство: открыв её, страница
показывает не cookie, а сам постоянный токен — единственный раз, когда он виден
вообще. Контроллер шлёт этот токен заголовком и умеет ровно один маршрут —
`GET /api/gate/status`; на обычные детские маршруты он не проходит, потому что
лежит файлом на диске той же машины. Файл настройки остаётся с правами `0600`:
теперь в нём не только refresh token Microsoft, но и ключ от детского аккаунта.
Отозванное устройство перестаёт быть предъявителем на том же запросе и получает
401; 403 остаётся ответом живому агенту, постучавшемуся не в `gate/status`.
Обоим контроллер отвечает как при недоступном Edukator — доступ не снимается.

Уже настроенный контроллер после обновления не поднимется: в его конфигурации
нет `agent_token`, а умолчания у него и у `edukator_url` теперь нет намеренно —
молчаливый `localhost` без токена дал бы вечный fail-closed вместо внятной
жалобы. Перезапустите `npm run family:login`: он предложит прежний адрес,
оставит прежний токен по Enter и **сохранит** настроенные `poll_seconds`,
`verify_seconds` и `block_days`.

На `/parents` родитель может временно разблокировать или заблокировать компьютер
до следующей московской полуночи либо вернуть автоматический режим «По плану».
Команда не меняет автоматический расчёт `automaticUnlocked` в
`GET /api/gate/status`; итоговое состояние находится в `unlocked`, и контроллер
применяет его при следующей сверке. Из родительской сессии PIN не спрашивается
вовсе: она сама и есть доказательство родительства. PIN нужен ровно для случая
«родитель подошёл к машине ребёнка»: с детской сессии сводка читается свободно,
а меняется только с PIN. Такой PIN остаётся в состоянии React текущей вкладки и
браузером не сохраняется. Без настроенного PIN или без `EDUKATOR_PIN_PEPPER` от
16 знаков панель видна с детской машины, но управление отключено.

Во время `family:login` нужно открыть показанную ссылку, войти родительским
Microsoft-аккаунтом, вставить полный redirect URL и выбрать точный ID ребёнка.
Refresh token сохраняется с правами `0600` в
`~/.config/edukator/family-safety.json`; в репозиторий он не попадает.
Если Microsoft Family Safety временно недоступен уже после входа, полученная
сессия сохраняется: повторный `npm run family:login` продолжит настройку без
нового OAuth-кода.

Контроллер использует закрытый мобильный API Family Safety, поэтому после
изменений со стороны Microsoft может потребоваться обновление. Если Edukator
недоступен, контроллер не снимает блокировку. Для экстренного ручного доступа
можно использовать `/parents`; если недоступен сам Edukator, остановите общий
запуск и снимите ограничение в приложении Family Safety.

В режиме разработки откройте `http://localhost:5173`: Vite обновляет интерфейс
без перезапуска и проксирует `/api` на сервер. `EDUKATOR_WEB_DEV=1`, которую
ставит команда `dev`, отключает раздачу production-статики сервером. Для
обычного запуска сначала выполните `npm run build:web`, затем откройте
`http://localhost:3000`; без `web/dist/index.html` сервер завершится с подсказкой
о сборке вместо пустой страницы.

`./run.sh` запускает сервер на `192.168.100.141:3000` вместе с уже настроенным
Family Safety-контроллером и требует заданного `EDUKATOR_PIN_PEPPER`: сам PIN
переменной больше не задаётся, он лежит хешем в `control.db`. Для прямого
`npm start` адрес по умолчанию — `0.0.0.0`; его можно переопределить переменной
`HOST`, а порт в обоих случаях — переменной `PORT`. Vite слушает
`localhost:5173`.

Учтите: cookie входа несут префикс `__Host-`, а его браузер принимает только с
`Secure`, то есть по HTTPS (кроме `localhost`, который браузер и так считает
доверенным источником). По голому http на адрес вида `192.168.100.141:3000`
войти не выйдет даже с `EDUKATOR_INSECURE_COOKIES=1`: этот выключатель снимает
`Secure`, но имя cookie не меняет. Заводите сервер за обратным прокси с
сертификатом либо ходите на него с той же машины по `localhost`.

Рабочий путь ученика:

1. При первом входе заполнить имя и имя напарника на экране знакомства.
2. Если по предметам ещё нет достаточной истории обычных забегов, пройти триаж
   хотя бы по одному из них. Он задаёт до 12 вопросов по разным темам без
   подсказок, меняя сложность на один уровень после ответа, и в конце ранжирует
   затронутые темы от слабых к сильным.
3. Если появился блок «Разобрать слабое место», сначала открыть отмеченную
   обязательную карточку, прочитать материал и пройти пять вопросов без
   подсказок. Четыре верных ответа дают зачёт. После незачёта нужно перечитать
   ту же теорию и повторить те же пять вопросов; повторы продолжаются до 4/5.
   Только первая попытка начисляет XP и меняет mastery, прогноз и учебную
   статистику. Остальные карточки остаются добровольными.
4. Выбрать карточку плана дня и пройти забег из 12 заданий. Между заданиями
   могут меняться темы одного предмета. В начале есть три жизни: неверный ответ
   можно исправить, и жизнь тратится только при отправке исправления. Пропуск
   исправления жизнь не тратит; после трёх ретраев ошибка окончательна.
   Исправление не считается новым вопросом, а итог и XP определяются последней
   версией ответа. Перед зачётом забега сервер проверяет осмысленность ответов:
   сомнительный вопрос нужно повторить, либо родитель может подтвердить его в
   детализации занятия. Финал показывает закрытые и просевшие темы и изменение прогноза.
5. Когда освоение темы станет выше 0.75 и фоновый воркер подготовит набор,
   пройти босса: пять свежих заданий подряд без подсказок. Только 5 из 5
   окончательно закрывают тему; после ошибки можно оспорить ответ или признать
   поражение и дождаться нового набора без календарного штрафа.
6. Имя, интересы, дату экзамена и имя напарника можно изменить в профиле;
   новые интересы попадут в следующий промпт генератора без перезапуска.
7. Родительскую сводку открыть напрямую по `/parents`. Ссылки из ученического
   интерфейса нет, но и секрета из неё не делают: ученик видит ту же страницу со
   своего устройства, а родитель — из своей сессии, с переключателем детей.

Те же возможности доступны по HTTP:

| Маршрут | Что делает |
|---|---|
| `GET /api/health` | состояние управляющей базы, карты тем и открытых детских баз |
| `GET /api/auth/me` | кто предъявитель: родитель, ребёнок, оба или никто; токенов и чужих адресов не отдаёт |
| `POST /api/auth/parent/login`, `POST /api/auth/parent/logout` | вход по email и паролю, выход |
| `POST /api/auth/persona` | переключить активную роль там, где в одном браузере живы обе сессии: `{ kind: "child" }` — свободно, `{ kind: "parent", password }` — с подтверждением пароля |
| `GET /api/auth/parent/invite/:token` | проверить приглашение, **не гася** его |
| `POST /api/auth/parent/invite/:token` | погасить приглашение, поставить пароль и получить сессию |
| `POST /api/auth/child/claim/:token` | погасить ссылку устройства на детской машине |
| `GET /api/family` | дети, их устройства и признак настроенного PIN |
| `POST /api/family/children` | завести ребёнка вместе с его базой |
| `POST /api/family/children/:id/provision` | повторить заведение базы ребёнка, застрявшего в `provisioning`/`failed` |
| `POST /api/family/children/:id/devices` | выпустить устройству одноразовую ссылку: браузеру она даёт cookie, агенту — постоянный токен |
| `POST /api/family/devices/:id/revoke` | отозвать устройство |
| `POST /api/family/pin` | сменить родительский PIN |
| `GET /api/run/plan` | план дня, `learning[]`, прогнозы, триаж, серия и состояния тем/боссов |
| `GET /api/gate/status` | гейт `{ completed, remaining, learning, automaticUnlocked, override, unlocked }`: автоматический расчёт, активная команда и эффективный итог |
| `POST /api/run/start` | `{ subject, topic_id }` → начать или продолжить забег по выбранной карточке плана |
| `POST /api/run/:id/finish` | закрыть забег или триаж и получить итог |
| `GET /api/integrity/:runId` | состояние проверки ответов текущего ребёнка |
| `POST /api/integrity/:runId/retry/:itemId` | повторить отмеченный вопрос: `{ answer, duration_ms, hint_used? }` |
| `POST /api/triage/start` | `{ subject }` → начать или продолжить триаж |
| `GET /api/triage/:id/next` | следующее диагностическое задание без подсказки |
| `GET /api/boss/topics` | карта тем и готовность босса каждой темы |
| `POST /api/boss/start` | `{ topic_id }` → начать или продолжить готовый бой |
| `GET /api/boss/:id/next` | следующее из пяти заданий боя, без подсказки |
| `GET /api/boss/:id/state` | сохранённый исход/прогресс боя для восстановления после перезагрузки |
| `POST /api/boss/:id/answer` | `{ task_id, answer, duration_ms?, hint_used? }` → результат ответа, XP, исход и прогресс боя |
| `POST /api/boss/:id/concede` | признать поражение после ошибки и заказать новый набор |
| `GET /api/learning/:id` | опубликованное структурированное содержимое и прогресс теста |
| `POST /api/learning/:id/open` | идемпотентно открыть или продолжить чтение |
| `POST /api/learning/:id/test` | возобновить незавершённый или создать следующий пронумерованный `lesson`-забег с теми же пятью заданиями |
| `POST /api/learning/run/:runId/finish` | атомарно завершить попытку; незачёт оставляет материал `active`, зачёт переводит его в `passed` |
| `GET /api/session/next` | задание по выбору планировщика; в забеге — `?runId=<id>`. Обычно эталон и разбор скрыты; при ожидающем исправлении `retry` восстанавливает ошибочный ответ и его разбор |
| `POST /api/session/answer` | `{ task_id, answer, runId?, hint_used?, duration_ms?, retry_attempt_id? }` → вердикт, XP, прогресс, разбор, шутка и новое состояние темы; `answer` длиннее 500 символов — 400 |
| `POST /api/session/retry/skip` | `{ runId, task_id }` → отказаться от доступного исправления без списания жизни |
| `POST /api/session/dispute` | `{ attempt_id }` → спор в очередь фоновой перепроверки: 202 при заведении, 200 с текущим `status` при повторном нажатии; нецелый или неположительный `attempt_id` — 400, неизвестный — 404 (`attempt-not-found`), уже засчитанная попытка — 400 (`attempt-correct`: спорить не о чем) |
| `GET /api/profile`, `PUT /api/profile` | прочитать или изменить профиль и текст знакомства |
| `GET /api/parents/:childId` | единый снимок родительского дашборда за семь суток с обязательным `computerAccess` и признаком `configured` |
| `GET /api/parents/:childId/runs/:runId` | вопросы, ответы и решения проверки конкретного занятия ребёнка |
| `PUT /api/parents/:childId/computer-access` | смена режима `{ mode: "automatic" | "blocked" | "unlocked" }`: из родительской сессии без PIN, с детской — с PIN |
| `PUT /api/parents/:childId/runs/:runId/integrity/:itemId/approve` | вручную подтвердить осмысленный ответ: родительской сессии PIN не нужен, с детской машины нужен |

Формы успешных ответов:

- `POST /api/run/start` с `{ subject: "math" | "russian" | "english",
  topic_id: "math.fractions" }` и `POST /api/triage/start` с `{ subject }` →
  `{ runId, resumed, progress }`;
- `GET /api/session/next?runId=<id>` → `{ task: { id, topic_id, topic_title,
  subject, question, hint, difficulty, answer_format }, progress: { total,
  correct, target, done, lives? } }`; у обычного забега `lives` содержит
  `{ total: 3, remaining, retryAvailable }`. При ожидающем исправлении ответ
  дополнительно содержит `{ retry: { attempt_id, previous_answer, answer,
  explain, joke, dispute_status? } }`; без `runId` поле `progress` отсутствует,
  а для `kind = lesson` поле `hint` отсутствует;
- `POST /api/session/answer` → `{ attempt_id, correct, normalized, reason?,
  answer, explain, joke, xp, progress, topic: { id, mastery, attempts,
  next_review } }`, где `progress` равен `null` вне забега, а
  `reason` — `mismatch`, `empty`, `no-number` или `ambiguous-number`;
- `POST /api/session/dispute` → `{ dispute_id, status, progress?, xp? }`, где
  `status` — `open`, `upheld` или `rejected`; подтверждённый спор обычного
  забега возвращает обновлённые progress и XP;
- `GET /api/triage/:id/next` → `{ status: "ok", task, progress }` либо
  `{ status: "done", total, target }`;
- `POST /api/boss/start` → `{ batchId, runId, resumed }`; следующий вопрос
  содержит `{ batchId, runId, position, task }`, а ответ — `{ attemptId,
  correct, normalized, answer, explain, joke, xp, outcome, progress }`;
- `POST /api/boss/:id/concede` → `{ runId, batchId, replacementBatchId }`;
- `GET /api/run/plan` включает `learning[]`: `{ id, subject, topic,
  recommendationReason, estimatedMinutes, status }`, где наружу выходят только
  состояния `ready | active`, а `gate.learning` имеет форму
  `{ materialId, required, passed }`; `gate.automaticUnlocked` истинно при трёх
  обычных забегах и выполненном либо отменённом обязательстве разбора,
  `gate.override` содержит активную команду до московской полуночи или `null`,
  а `gate.unlocked` — эффективный итог с учётом этой команды;
- `GET /api/learning/:id` → `{ id, subject, topic, recommendationReason,
  estimatedMinutes, status, content, progress }`; `content` содержит
  `introduction`, 1–3 `objectives`, 3–5 `sections` с блоками
  `paragraph | formula | example | warning` и итоговый `summary`;
- `POST /api/learning/:id/open` → `{ materialId, resumed, material }`,
  `POST /api/learning/:id/test` → `{ materialId, runId, resumed, progress }`;
- `POST /api/learning/run/:runId/finish` → обычный итог забега плюс
  `{ materialId, required, outcome, masteryBefore, masteryAfter }`; зачёт
  `passed` начинается с 4/5, меньший счёт даёт `failed` только как исход
  запуска, оставляя сам материал `active` для нового запуска;
- `POST /api/run/:id/finish` → `{ runId, total, correct, xp, touchedTopics,
  closedTopics, declinedTopics, forecast, forecastDelta? }`;
- `GET /api/profile` → `{ name, interests, examDate, partnerName,
  introduction }`; `PUT /api/profile` принимает частичный patch этих полей
  (кроме `introduction`) и отвергает неизвестные поля.
- `GET /api/gate/status` → `{ day, required, completed, remaining, learning,
  automaticUnlocked, override, unlocked }`, где `override` равен `null` либо
  `{ mode: "blocked" | "unlocked", changedAt, expiresAt }`;
- `GET /api/parents/:childId` → прогнозы по предметам, план и факт активного
  времени, семь дневных значений, до пяти проблемных тем, ленту завершённых
  забегов, флаги наблюдений и обязательный `computerAccess` в форме gate плюс
  `configured` — настроен ли родительский PIN. В ответе нет внутренних `topic_id`;
- `PUT /api/parents/:childId/computer-access` принимает ровно
  `{ mode: "automatic" | "blocked" | "unlocked" }` и возвращает актуальный gate.
  Родительской сессии PIN не нужен; с детской сессии нужен
  `Authorization: Bearer <PIN>`. Некорректный body даёт 400, неверный PIN — 401,
  серия неудач — 429 с паузой в 15 минут (пять ошибок на учётную запись, двадцать
  на адрес), ненастроенный PIN, отсутствующий pepper, недоступный счётчик или
  недоступная база — 503;
- `GET /api/auth/me` → `{ kind: "anonymous" }`, `{ kind: "parent", email }`,
  `{ kind: "agent", childId }`, `{ kind: "child", childId, name }` либо
  `{ kind: "both", active: "parent" | "child", parent: { email },
  child: { childId, name } }` — последнее, когда в одном браузере живы обе
  сессии; `active` показывает сохранённый выбор роли. Детскому
  предъявителю ни родительского адреса, ни токенов не видно: за детской машиной
  сидит ученик, и учётная запись родителя — не его дело. Под заходом оператора
  ответ приходит от **целевой** семьи и несёт `impersonation: { adminEmail,
  childName, role, expiresAt }`; заход проверяется раньше собственных cookie
  оператора, иначе полоса не появлялась бы ровно там, где нужна. 401 здесь не
  бывает: «никто не вошёл» — обычное состояние страницы входа, а не ошибка;
- `POST /api/auth/persona` с `{ kind: "child" }` или `{ kind: "parent",
  password }` → тот же ответ `both` с новым `active` и cookie
  `__Host-edu_actor` (`HttpOnly`, `SameSite=Strict`). Cookie прав не даёт: обе
  сессии проверяются серверно на каждом запросе, а выбор роли `parent`
  подтверждается паролем — это единственное место, где родительская сессия
  переспрашивается. Живы не обе сессии — 409;
- `POST /api/auth/parent/login` с `{ email, password }` →
  `{ kind: "parent", email }` и cookie `__Host-edu_parent`;
  `POST /api/auth/parent/logout` → `{ kind: "anonymous" }` и погашенная cookie;
- `GET /api/auth/parent/invite/:token` → `{ email }`;
  `POST` того же адреса с `{ password }` → `{ kind: "parent", email }` и cookie.
  Короткий пароль даёт 400, погашенная или протухшая ссылка — 404;
- `POST /api/auth/child/claim/:token` → `{ kind: "child", childId }` и cookie
  `__Host-edu_child` для браузера, либо `{ kind: "agent", childId, token }` для
  контроллера. Это **единственный** ответ, где постоянный токен устройства виден
  вообще, и виден он ровно один раз: в базе лежит отпечаток;
- `GET /api/family` → `{ email, pinConfigured, children: [{ id, name, status,
  createdAt, devices: [{ id, kind, label, claimedAt?, revokedAt?,
  inviteExpiresAt }] }] }`;
- `POST /api/family/children` с `{ name }` → заведённый ребёнок;
  `POST /api/family/children/:id/provision` → тот же ребёнок с готовой базой.
  Общий `authorizeChild` здесь намеренно не используется: он допускает только
  `ready`, а повтор нужен как раз `provisioning` и `failed`;
  `POST /api/family/children/:id/devices` с `{ kind, label }` →
  `{ device, invite: { path, token, expiresAt } }`, где `token` — токен
  **приглашения** и виден один раз;
  `POST /api/family/devices/:id/revoke` → `{ revoked, device }`, где `revoked`
  показывает, сработал ли именно этот запрос: повторный отзыв не ошибка;
- `POST /api/family/pin` с `{ pin }` → `{ pinConfigured: true }`;
- отказы допуска общие для всех арендных маршрутов: нет или недействителен
  предъявитель — 401, этому виду сюда нельзя — 403, изменяющий запрос без
  подтверждённого источника — 403, чужой или несуществующий ребёнок — **один и
  тот же** 404 (разные тексты превратили бы перебор `id` в справочник заведённых
  детей), потолок открытых баз или недоступная база ребёнка — 503.

Стрик считает только завершённые обычные забеги (`kind = "run"`), а не триаж,
персональный тест или босса. День определяется в `Europe/Moscow`;
незавершённый сегодняшний день не обрывает серию, которая дошла до вчера.

Персональный материал рассчитан на 10–15 минут и повторяется до зачёта 4/5.
Незавершённая попытка возобновляется прямо в тесте. Завершённый незачёт снова
открывает ту же теорию, после чего создаётся новый `lesson`-запуск с теми же
пятью вопросами; только ответы первого запуска влияют на учебный прогресс.
Таймера и отслеживания прокрутки нет. Формулы рендерятся существующим
KaTeX с `trust: false`, произвольный HTML в содержимом запрещён. Спор использует
обычный асинхронный механизм и блокирует завершение, пока открыт; клиент и
сервер запрещают подсказки в `lesson`-забеге.

Босс доступен строго при `mastery > 0.75`. Он состоит ровно из пяти заданий без
подсказок и закрывает тему навсегда только при `correct === total === 5`.
Подтверждённая ошибка заканчивает бой без штрафа к XP или искусственного
снижения mastery; открытый спор ставит бой на паузу. Задания босса готовятся
отдельно от обычной очереди, но проходят тот же разбор, независимую проверку,
дедупликацию и атомарную запись банка.

`/parents` использует скользящее окно в семь суток. План фиксирован на 630
минут, а факт — это сумма `attempts.duration_ms`, то есть активная работа, а не
время открытой вкладки. Флаги — наблюдения для разговора, не рекомендации по
контролю: бездействие означает три полных московских календарных дня, плато —
пять суток с занятиями и минимум двумя сравнимыми снимками прогноза.

`hint_used`, если передан, обязан быть логическим; `duration_ms` — безопасным
целым числом миллисекунд не меньше нуля.

Первичный ответ на задание принимается ровно один раз и только по выданному заданию:
выдача привязана к конкретному `runId`, поэтому другой забег того же предмета
не может присвоить её себе. Триаж не принимается обычным `session/next` и не
разрешает `hint_used: true`. Завершение до 12 ответов, до фактического исчерпания
триажа или при открытом споре отвечает 409 (`run-not-ready`). После
12-го уникального ответа обычный забег готов к завершению только после закрытия
ожидающего исправления. Исправление создаёт новую текущую версию попытки, не
увеличивает `runs.total` и наследует использованную подсказку; семантические
расчёты используют только текущую версию, а родительское активное время — все
версии. Без корректного `retry_attempt_id` повторный ответ даёт 409, задание из
очереди, которое ученику не выдавали, — тоже 409
(`task-not-issued`), а `id`, которого в банке нет вовсе, — 404. Когда предлагать
нечего, `next` перебирает весь план тем и только потом отвечает 503 — 404
читалось бы как ошибка клиента. Одной пустой темы для отказа мало: ответить по
ней ученик не может, состояние её не меняется, и планировщик предложил бы её же
снова; а посев покрывает шесть тем предмета из двадцати с лишним, так что
фиксированное окно из нескольких тем регулярно целиком состоит из пустых.
Тема, задание которой не читается (повреждённый `accept[]`), пропускается с
записью в stderr, а не роняет запрос: строка «выдано, но не отвечено» находится
снова и снова, и пятисотка была бы вечной. Уже выданное задание, которое нечем
сверить, `answer` по той же причине помечает `rejected` и отвечает 409
(`task-defective`), а не 500: причина уходит в stderr, потому что называет
эталонный ответ. Уже выданное, но неотвеченное задание `next` отдаёт
повторно — перезагрузка страницы не должна сжигать очередь. При неподнятой базе
или карте тем все три маршрута отвечают 503 с причиной; подробности — в
`/api/health`. Поломка внутри отвечает 500 с общим текстом: подробности с
локальными путями уходят в stderr, а не в тело ответа.

Подтверждённый спор пересчитывает модель знаний с нуля по всем попыткам темы и
дописывает ответ ученика в `accept[]` задания. Попытка засчитывается всегда, а
вот в `accept[]` ответ попадает не любой: пустая запись, «45 и 46» на числовой
теме, запись со знаком процента и дубль уже имеющегося варианта отбрасываются —
такая запись сделала бы посевной файл предмета неразбираемым навсегда либо
принимала бы долю вместо числа. Разбор идёт фоном и занимает
минуты: вердикт приезжает не в ответе на `dispute`. Если разбор не удался (codex
недоступен, ответ модели не разобрался), спор остаётся `open`, и повторное
нажатие кнопки ставит его на разбор заново. Пока разбор идёт, повторное нажатие
только отдаёт текущий статус: иначе каждое из них поднимало бы ещё один процесс
codex на минуты. У споров есть **один отдельный слот codex**, который
фоновый воркер занять не может. Поэтому спор стартует сразу, даже если заняты оба
места прогрева. Второй одновременный спор остаётся открытым и повторяется автоматически.

Без предъявителя не отвечает ни один арендный маршрут. Предъявителей четыре:
**родитель** (cookie `__Host-edu_parent`, `SameSite=Strict`, выдаётся по
паролю), **браузер ребёнка** (cookie `__Host-edu_child`, `SameSite=Lax`,
выдаётся погашением ссылки), **агент** (токен устройства заголовком
`Authorization: Bearer`, ровно один маршрут `GET /api/gate/status`) и
**оператор** (cookie `__Host-edu_admin`, `SameSite=Strict`, выдаётся по паролю
оператора; аренды у его маршрутов нет вовсе — см. «Оператор»). Агент — не
детская сессия: он лежит файлом на детской машине, и допуск его к обычным
маршрутам означал бы, что этот файл умеет сдавать ответы. Оператор — не
родитель: ни одна строка матрицы значением `admin` не пополняется. `SameSite` за
защиту от CSRF не считается: изменяющие запросы дополнительно требуют совпадения
`Origin` либо `Sec-Fetch-Site: same-origin`. Cookie идут с `Secure`, который
снимается только явным `EDUKATOR_INSECURE_COOKIES=1` для локального http.

| Маршруты | родитель | ребёнок | агент | оператор |
|---|---|---|---|---|
| `/api/session`, `/api/run`, `/api/triage`, `/api/boss`, `/api/learning`, `/api/profile` | нет | да | нет | нет |
| `GET /api/gate/status` | нет | да | **да** | нет |
| `GET /api/parents/:childId` | да | да, только свой | нет | нет |
| `PUT /api/parents/:childId/computer-access` | да, без PIN | да, свой и с PIN | нет | нет |
| `/api/family/*`, `/api/auth/parent/*` | да | нет | нет | нет |
| `/api/admin/*`, `/api/auth/admin/*` | нет | нет | нет | **да** |

Под заходом оператора (`__Host-edu_impersonation`) предъявителем становится
родитель или браузер **целевой** семьи — но только на чтение. Изменяющий запрос
отвергается 403 с кодом `read-only`, и это касается и `/api/family/*`, у которых
аренды нет: состав семьи, устройства и PIN лежат в `control.db`, куда
`PRAGMA query_only` не достаёт, поэтому первый замок у них выписан отдельно.

Чтение сводки ребёнком — не послабление, а исполнение принципиального
ограничения: ученик видит тот же дашборд и знает о нём.

Перебор пароля и PIN ограничен счётчиками в `control.db` — по учётной записи и
по адресу клиента, с паузой; при недоступном счётчике вход не пускается вовсе.
Адрес клиента берётся из `X-Forwarded-For` **только** от перечисленных в
`EDUKATOR_TRUSTED_PROXIES` адресов (по умолчанию — loopback): иначе клиент
подставлял бы новый заголовок на каждую попытку и обходил счётчик. Список
пишется через запятую или пробел (`EDUKATOR_TRUSTED_PROXIES='10.0.0.2, ::1'`) и
состоит из **адресов, а не сетей**: CIDR не разбирается. Незаданная переменная
означает loopback, а **заданная пустой** — не верить заголовку ни от кого.

Всё это не заменяет TLS. Публикуя сервер наружу, ставьте его за обратный прокси
с сертификатом и не открывайте порт приложения напрямую.

Прокси обязан **сохранять заголовок `Host`**. Изменяющий запрос подтверждает
источник совпадением всего `Origin` — схемы и хоста. В production принимается
только `https://<Host>`; голый HTTP разрешён лишь для loopback и при явном
`EDUKATOR_INSECURE_COOKIES=1`. `Sec-Fetch-Site: same-origin` не исправляет
неверную схему или подменённый хост прокси. `proxy_pass` в nginx по умолчанию
подменяет `Host` адресом сервера (`proxy_set_header Host $host;` возвращает
исходный; Caddy и Traefik сохраняют его сами). С подменённым `Host` страницы
открываются, а вход, погашение ссылки, ответ ученика и смена PIN отвечают 403
«Изменяющий запрос без подтверждённого источника» — поломка выглядит клиентской,
хотя дело в конфигурации прокси.

Проверка живости:

```sh
curl http://192.168.100.141:3000/api/health
# {"status":"ok","version":"0.1.0","maintenance":false,
#  "control":"ok","curriculum":"ok","catalog":{"state":"idle",...},
#  "children":{"open":1,"detached":[]}}
```

### Деплой `edukator.ru`

Из чистого Git worktree запустите:

```sh
./deploy.sh
```

Скрипт выполняет обязательные проверки проекта, собирает `web/dist`, упаковывает
текущий `HEAD` и отправляет релиз на `root@195.133.56.188`. На сервере новая
версия сначала готовится отдельно в `/opt/edukator`: `npm ci` работает от
пользователя `edukator` и получает HTTP(S)-прокси из
`/etc/edukator/edukator.env`. До переключения он снимает согласованную копию
всех баз и catalog artifacts в `/var/lib/edukator/deploy-backups/<release-id>`.
До загрузки и остановки сервиса `deploy.sh` read-only проверяет OCR-зависимости;
пакеты обычный deploy не устанавливает. После этого скрипт
останавливает `edukator.service`, заменяет `/opt/edukator/app` и проверяет внутренний
`/api/health`. На первом старте лежит `EDUKATOR_DATA_DIR/.maintenance`: сервер
разрешает health и миграции, но отвергает пользовательские записи. При
неуспешном health новая версия останавливается, manifest снимка проверяется,
базы и catalog artifacts восстанавливаются, и только после этого запускается
предыдущий код. Три последних заменённых каталога остаются в
`/opt/edukator/releases`, три последних снимка данных — в каталоге
`deploy-backups`.

Перед первым production-деплоем установите зависимости отдельно:

```sh
ssh root@195.133.56.188 /bin/bash -s < scripts/install-ocr-dependencies.sh
```

Каталог данных `/var/lib/edukator/data`, серверный env и авторизация Codex в
релиз не входят. Одновременные деплои запрещает `/opt/edukator/.deploy.lock`.
SSH должен работать по ключу без интерактивного ввода.

Значения по умолчанию можно переопределить окружением:

```sh
EDUKATOR_DEPLOY_HOST=example.org \
EDUKATOR_DEPLOY_USER=root \
EDUKATOR_DEPLOY_KEEP_RELEASES=5 \
./deploy.sh
```

### База и резервное копирование

Баз не одна, а столько, сколько детей. Всё лежит в **каталоге данных** — `data`
рядом с кодом, переопределяется переменной `EDUKATOR_DATA_DIR`:

```
data/
  control.db          # аккаунты, каталог/редакции, назначения, OCR jobs и FTS
  children/<id>.db    # занятие одного ребёнка: та же схема, что была раньше
  catalog/artifacts/  # исходные PDF и оптимизированные изображения OCR-страниц
  catalog/tmp/        # временные upload/OCR/delete файлы
  logs/app.jsonl      # аварии для админки: 4 файла по 8 МБ, ротация по размеру
  edukator.lock       # замок каталога: одно живое приложение на каталог
```

```sh
EDUKATOR_DATA_DIR=/srv/edukator/data npm start
```

Если процесс погиб без штатного закрытия, `edukator.lock` остаётся на месте и
следующий запуск отказывается работать с каталогом. Проверьте указанный в ошибке
PID и, только убедившись, что владельца больше нет, удалите этот файл вручную.
Автоматическое снятие здесь небезопасно: два одновременных запуска могут принять
один старый замок за свободный и оба начать работу.

Путь базы ребёнка нигде не хранится: он вычисляется из непрозрачного `id`,
канонизируется и проверяется на принадлежность `children/`. Хранимый путь был бы
вторым источником истины и прямой дорогой к открытию, скажем, самого
`control.db` как «детской базы». Пустое значение `EDUKATOR_DATA_DIR=` считается
незаданным, а не текущим каталогом: иначе базы появлялись бы там, откуда сервер
запустили.

Хранилище обязано поддерживать SQLite WAL: при открытии приложение проверяет,
что `PRAGMA journal_mode=WAL` действительно вернул `wal`, и иначе отказывается
запускаться. Поэтому `:memory:` и файловые системы, на которых WAL недоступен,
для каталога данных не подходят.

У `control.db` **своя** версия схемы и своя миграция, не связанные с версией
детской базы: схемы живут своими циклами. Сейчас это **v3**. Помимо аккаунтов,
детей и админского аудита здесь находятся `courses`, `course_revisions`,
`topics`, `revision_topics`, `topic_prereqs`, `course_sources`, `source_pages`,
`source_chunks` с contentless FTS5, `revision_topic_sources`, `catalog_jobs`,
`child_courses` и `child_topic_exclusions`. Пароли и PIN хранятся `scrypt`-хешем с индивидуальной солью
(PIN — ещё и с серверным pepper), токены — только SHA-256-отпечатком от
256-битной случайности. Открытых токенов в базе нет вовсе, поэтому показать
выпущенную ссылку второй раз нельзя.

Схема детской базы — **v19**. Основные таблицы: `profile`, `topic_state`,
`task_bank`, `runs`, `attempts`, `disputes`, `forecast_snapshots`,
`boss_batches`, `boss_tasks`, `learning_materials`, `learning_runs`, `learning_tasks`,
`computer_access_override`, `integrity_reviews`, `integrity_items`. Последние две
хранят состояние проверки занятия и решения по отдельным ответам в базе именно
того ребёнка, которому принадлежат `runId` и `itemId`. `computer_access_override`
хранит singleton-команду `blocked | unlocked`
с временем изменения и сроком до следующей московской полуночи; после срока
дневной доступ снова определяется автоматически.
`runs.kind` допускает `run | triage | boss | lesson`, а `task_bank.status` — в
том числе `lesson_reserved`. `runs.lives_remaining` и `runs.retry_task_id`
хранят жизни и ожидающее исправление обычного забега; `attempts.is_current`
выбирает итоговую версию ответа, `attempts.life_charged` позволяет спору
вернуть ровно удержанную этой версией жизнь, а `attempts.affects_progress`
отделяет нейтральные ответы повторного разбора. `learning_materials` хранит статус
`preparing | ready | active | passed | failed | rejected | retired`, JSON
теории, причину рекомендации, оценку времени, `mastery_before`, момент
публикации `ready_at` и остальные временные отметки. Связи запусков вынесены в
`learning_runs(material_id, run_id, attempt_number)`. `learning_tasks` фиксирует ровно пять позиций 1–5 и их
порядок; внешние ключи и триггеры запрещают связать чужую тему или не-lesson
забег. Частичный уникальный индекс разрешает не больше одного живого материала
темы.

Схема домигровывается при каждом открытии, отдельной команды миграции нет — и у
управляющей базы, и у детских. База с более новым `user_version` отвергается:
откат приложения не поддерживается.

При первом старте после обновления `bootstrapLegacyCourses` идемпотентно
переносит файловые карты `math`, `russian`, `english` в каталог с сохранением
course/topic IDs и назначает их существующим детям. Повторный bootstrap не
создаёт дубли и не восстанавливает курс, который родитель снял уже после
миграции. Детские базы мигрируются лениво при первом открытии; исторические
runs, attempts, forecasts и task bank сохраняются. Во время deploy это окно
закрывает `.maintenance`, а predeploy snapshot позволяет вернуть и код, и
обе схемы, и артефакты вместе.

Отказы точечные: `control.db` гасит каталог и новые аренды, а испорченная база одного
ребёнка мешает только ему — остальные занимаются. Базы детей открываются по
первому обращению и остаются открытыми; вытеснения нет, потолок открытых баз
отказывает новому арендатору и не трогает уже открытых, иначе закрытие уводило
бы базу из-под идущей транзакции.

`GET /api/health` возвращает 503, если не поднялась управляющая база или каталог
тем, а также если файл **уже открытой** детской базы подменили после старта.
Новых баз health не открывает: это ровно та работа, которой ленивый реестр и
избегает. Ответ содержит только стабильные поля `status`, `version`, `control`,
`maintenance`, `control`, `curriculum`, `catalog` и
`children: { open, detached }`; подробности с локальными путями пишутся в
stderr. `catalog.state = degraded` и счётчик `failed` требуют внимания, но сами
по себе не останавливают занятия по уже опубликованным курсам.

Резервная копия снимается на живом сервере:

```sh
npm run backup -- --out ../backups/2026-08-19
```

Копия снимается через `VACUUM INTO` и ложится в тот же вид, что и оригинал
(`control.db`, `children/<id>.db`, каталог артефактов и
`catalog/manifest.json`). Manifest создаётся внутри backup и содержит SHA-256
и размер каждого учтённого PDF/изображения; он проверяется сразу после
копирования. Обходятся
**все** дети, включая выведенных: `retired_at` —
пометка в управляющей базе, а не удаление прогресса (команды и кнопки для вывода
ребёнка пока нет — пометка живёт в схеме и в бэкапе). Ребёнок, чьей базы не
оказалось на месте, попадает в отчёт, а не пропадает молча: у готового ребёнка
это уже потеря и код возврата 1, у незаведённого — законное отсутствие и лишь
строка в stderr. Испорченная база
одного ребёнка не отменяет копию остальных: она называется отдельной строкой и
кодом возврата 1 — падать на первом же ребёнке значило бы оставить каталог
полукопией, дописать которую повторный запуск даже не смог бы. Составом схемы
копия проверяется, только если её `user_version` совпадает с версией
приложения: детские базы мигрируются лениво, и снимок ребёнка, который не
занимался с прошлого обновления, законно приезжает со схемой прошлой версии.
Целость от этого не страдает — `quick_check` идёт на каждой копии. Копия
управляющей базы открывается **только на чтение**: `openControlDatabase` включил
бы WAL и прогнал бы миграцию, то есть снимок, снятый перед обновлением, оказался
бы уже обновлённым. Простым `cp` пользоваться нельзя: под WAL часть
зафиксированных транзакций лежит в спутнике `-wal`, и копия одного файла тихо
теряет последние занятия.

Следите за свободным местом с учётом исходного PDF, временного OCR PDF,
изображений страниц и резервной копии: на время обработки и deploy нужно
несколько размеров исходного документа сверх постоянного каталога. Upload по
умолчанию ограничен 100 МиБ и 2000 страницами. `catalog/tmp` не является
бэкапируемым артефактом. Незавершённые операции удаляют свои temp-файлы сами;
для плановой очистки repository API `CourseArtifactStore.cleanupUnused(date)`
удаляет только старые temp/orphan-файлы и failed-источники редактируемых drafts,
на которые не ссылаются темы или jobs. Отдельного CLI для этой операции пока
нет: не удаляйте `catalog/artifacts` вручную и не чистите опубликованные
редакции.

### API каталога и эксплуатационная диагностика

Админские маршруты требуют отдельную admin cookie и недоступны родителю,
ребёнку и оператору в режиме impersonation:

- `GET|POST /api/admin/courses` — список и создание;
- `GET|PATCH /api/admin/courses/:courseId` — карточка и metadata draft;
- `GET|POST /api/admin/courses/:courseId/draft` — чтение/клонирование draft;
- `PUT /api/admin/courses/:courseId/draft/topics` — атомарная замена графа с
  optimistic `editVersion`;
- `GET|POST /api/admin/courses/:courseId/draft/build` — статус/запуск drafting;
- `POST /api/admin/courses/:courseId/publish|archive` — публикация/архивирование;
- `GET|POST /api/admin/courses/:courseId/sources` и
  `DELETE .../sources/:sourceId` — PDF draft;
- `GET .../sources/:sourceId/status` и `POST .../retry` — OCR и retry диапазона.

Родителю доступны `GET /api/family/courses`, назначения
`PUT /api/family/children/:childId/courses/:courseId` с
`{ "excludedTopicIds": [...] }` и снятие назначения тем же адресом через
`DELETE`. Черновики, архивные курсы и источники в семейный API не попадают.

Диагностику начинайте с `/api/health`, затем с экрана курса в `/admin`:
`catalog.queued/running/failed`, source status, номер страницы и текст ошибки.
Повторяйте только failed source или нужный диапазон страниц. При ручном smoke на
подготовленной Ubuntu выполните `./scripts/install-ocr-dependencies.sh --check`
и `npm run test:ocr`; тест использует маленький русский scan fixture и
намеренно не входит в обычный `npm test`.

Профиль настраивается через экран или `GET`/`PUT /api/profile`. Не правьте его
SQL вручную: `writeProfile` валидирует дату и сериализует интересы, а маршрут
сохраняет этот единый контракт.

### Программное использование ядра

Детскую базу открывают только через `openDatabase` (внутри сервера — через
реестр арендаторов, а не по пути), управляющую — через `openControlDatabase`.
`TenantRegistry` получает персональный снимок от `CurriculumProvider` и
синхронизирует разрешимые темы с `topic_state`. Доменные модули про `control.db`
не знают и его соединения не
получают: они видят ровно одну детскую базу, как и раньше. `checkAnswer` не пишет в
базу. `recordAttempt` атомарно обновляет только состояние освоения темы; строку
истории в `attempts` с `task_id` и `run_id` обязан записать вызывающий сценарий.
`planFromDatabase` читает профиль и начатые забеги, а `recordForecasts` пишет
общий и предметные снимки одной транзакцией. Жизненный цикл строки `runs`
принадлежит `startRun`/`finishRun`; занятие только связывает попытку с `run_id`
и атомарно обновляет счётчики.

## Проверки

```sh
npm test        # vitest, все tests/**/*.test.ts
npm run coverage
npm run typecheck
npm run lint
npm run build:web
npm run test:e2e   # Playwright: вход, семья, забег, спор, триаж, персональный разбор, босс, /parents и оператор
npm run family:test   # pytest контроллера Family Safety; npm test его не видит
```

Первые пять команд — обязательные гейты перед коммитом; `npm test` и
`npm run coverage` сами собирают интерфейс, но отдельный `build:web` остаётся
явной проверкой production-сборки. E2E обязателен для изменений пользовательских
потоков, HTTP-контрактов или запуска сервера, а `family:test` — для изменений в
`controller/`: `npm test` гоняет только Vitest и Python не трогает.

`coverage` держит порог 80% (строки, ветви, функции) **пофайлово** на ядре
(`normalize`, `mastery`, `scheduler`, `forecast`), занятии и данных (`session`,
`curriculum`, `db`, `json-schema`), забеге и триаже (`run`, `triage`, `xp`,
`session-error`), игровом слое (`streak`, `boss`, `boss-prep`, `parents`),
маршрутах, семафорах codex, генерации, многоарендности (`control-db`, `secrets`,
`parent-pin`, `auth`, `tenant-registry`, `data-dir`, `data-lock`,
`client-address`, `backup`, `dispute-coordinator`,
`routes/{auth,family,gate,tenant-context,token-privacy}`),
её клиентских экранах (`App`, `LoginScreen`, `FamilyScreen`, `InviteScreen`,
`JoinScreen`, `auth-api`, `family-api`, `http`), админке (`server/admin/**`,
`server/routes/admin/**`, `server/log.ts`, `server/tenant-opener.ts`,
`web/src/admin/**`, `web/src/admin-api.ts`, `web/src/app-route.ts`) и каркасе
(`index`, `run-child`, `atomic-write`). Пороги висят именно на них: всё это
детерминировано (внешние процессы подменяются через `run`/`produce`/`review`),
а дефект тихо портит учебный план, не роняя ни одного теста. Пофайлово, а не в
среднем по группе: иначе один модуль мог бы просесть до нуля, пока общая цифра
держится соседями. Скрипты извлечения PDF и сборки карты завязаны на сканы и на
codex, общий порог по репозиторию был бы враньём.

## Legacy-сборка файловых карт тем

Файлы `content/curriculum/` нужны для идемпотентного bootstrap исходных курсов
`math`, `russian`, `english` и совместимости старых установок. Они не являются
источником истины для новых курсов: новые курсы, PDF, OCR и редакции создаются
в админке и хранятся в `control.db` плюс `EDUKATOR_DATA_DIR/catalog`.

Ниже описан старый локальный конвейер обслуживания bootstrap-файлов. Он не
используется catalog worker и не ограничивает каталог тремя предметами или
фиксированным классом.

Конвейер из двух шагов.

**1. Оглавление учебника из PDF**

```sh
npm run extract-toc -- --subject math --pdf ~/Downloads/merzljak-6.pdf
```

| Флаг | Обязателен / значение по умолчанию | Смысл |
|---|---|---|
| `--subject` | да | `math`, `russian` или `english` |
| `--pdf` | да | путь к учебнику |
| `--pages` | поиск автоматически | страницы PDF с единицы, не печатные номера; `12` или `12-15`, диапазон обязан лежать внутри файла и отключает эвристику |
| `--scan` | `15` | положительное число страниц с каждого края |
| `--no-ocr` | OCR автоматический | запретить Vision OCR; с `--pages` также явно доверяет даже короткому текстовому слою |
| `--out` | `content/raw-toc` | каталог результата |

Результат перезаписывает `<out>/<subject>.txt`. Сначала проверяется найденное
оглавление в текстовом слое; OCR запускается, если подходящих страниц нет и хотя
бы одна из просмотренных страниц имеет слишком бедный слой. Это правило
действует и для ручного диапазона: богатый текст соседней страницы не скрывает
скан оглавления, а один номер страницы или штамп не считается оглавлением.
Чтобы намеренно сохранить короткий текстовый слой без OCR, укажите
`--pages ... --no-ocr`.

**2. Карта тем через codex**

```sh
npm run build-curriculum -- --subject math
```

| Флаг | Обязателен / значение по умолчанию | Смысл |
|---|---|---|
| `--subject` | да | `math`, `russian` или `english` |
| `--toc` | `content/raw-toc/<subject>.txt` | входное оглавление |
| `--out` | `content/curriculum` | каталог карты; рабочий `<subject>.json` атомарно заменяется только после валидации |
| `--attempts` | `3` | положительное целое число попыток |
| `--model` | модель роли `curriculum` | модель codex |

Скрипт требует 20-25 тем на предмет, прогоняет ответ через тот же валидатор,
что и файл из репозитория (схема, дубли `id`, ссылки в `prereqs`, циклы,
предпосылки вне экзамена, число тем в пределах 15-30), и при провале уходит на
следующую попытку, дописав текст
ошибки в промпт. После трёх неудач падает с отчётом по всем попыткам. В промпт
дописывается только то, что провалила сама модель: сорванный запуск codex
(ненулевой код возврата, не записанный ответ) идёт в отчёт, но не в промпт —
это диагностика CLI, а не замечание к ответу.

Распознанное оглавление уходит в промпт блоком JSON и режется по длине: это
недоверенный текст со скана, и текстовыми разделителями он умеет закрыть свой
раздел промпта и открыть собственный.

### Формат темы

Карты правятся руками, поэтому вот из чего состоит тема. Источник правды —
`schemas/curriculum.json`, файл проверяется тем же валидатором, что и ответ
модели (`npm test` прогоняет приёмочную проверку всех трёх карт).

| Поле | Значение |
|---|---|
| `id` | `<subject>.<латиницей-через-дефис>`. Менять нельзя: на него ссылаются `topic_state`, задания и попытки, смена `id` теряет накопленный прогресс |
| `subject` | `math` / `russian` / `english`, обязан совпадать с `subject` файла |
| `title` | название обычными словами — попадёт в дашборд родителей |
| `exam_weight` | 0-3, вероятность встретиться на вступительном. Главный рычаг приоритизации; при `< 1` тема не идёт ни в прогноз, ни в отставание предмета, а при `0` не попадает и в план |
| `difficulty` | 1-3, базовая сложность |
| `prereqs` | массив `id` тем того же файла, без циклов и дублей. Пока не каждая предпосылка достигла `mastery` 0.6, приоритет темы уменьшается мягким множителем `max(0.2, среднее(min(1, mastery / 0.6)))`, но не обнуляется. Предпосылкой не может быть тема с `exam_weight` 0: сама она никогда не попадёт в план |
| `answer_format` | `number` / `text` / `choice` — выбирает ветвь нормализатора |
| `prompt_seed` | как генерировать задания: что спрашивать, какие числа и слова брать, 2-3 примера формулировок целиком |

При `answer_format: number` формулировки в `prompt_seed` обязаны требовать **ровно
одно число**. «Найдите оба числа», «расположите в порядке возрастания»,
«разложите на простые множители», «запишите формулу», ответ вида `3:4` и ответы
словами дают ноль или несколько чисел, а такой эталон нормализатор считает
дефектом задания и падает — не в тестах, а на живом ответе ученика. Либо
переформулируйте вопрос под одно число, либо ставьте теме `text`. Смешанные
числа (`1 2/3`) и обыкновенные дроби (`3/4`) считаются одним числом и
допустимы.

Синхронизация карты ничего не удаляет. Удалённый или переименованный `id`
оставляет осиротевшую строку `topic_state` и предупреждение в stderr, но health
остаётся зелёным; возврат старого `id` снова подключит сохранённый прогресс.
Переименование поэтому оформляют как явную миграцию всех ссылок, а не как
обычную правку JSON.

## Генерация заданий

Задача этапа сформулирована двумя запретами: **ученик никогда не видит спиннер и
никогда не получает кривое задание**. Первое обеспечивает опережающая генерация,
второе — независимая перепроверка каждого задания вторым вызовом модели.

```
планировщик → активные темы → генератор (codex, батч 5)
                            → проверяющий (codex, видит только условия)
                            → сверка ответов через normalize.ts
                            → task_bank
```

Батч из 5 заданий с подсказкой, разбором и шуткой занимает у `gpt-5.6-sol`
около 29 секунд и покрывает 5-10 минут решения — поэтому очередь успевает
наполняться, пока ученик занимается, но только если её начали греть заранее.

### Модель по ролям

Используются пять ролей codex, и модель задаётся на каждую отдельно. Роли
`generate` и `validate` обслуживают не только обычный банк, но и подготовку
персональной теории и самостоятельных вопросов к ней. Разводить
роли приходится потому, что проверяющий решает задания независимо от генератора:
если он слабее, расхождение ответов он списывает на ошибку генератора и бракует
верные задания.

| Переменная | Роль | Умолчание |
|---|---|---|
| `EDUKATOR_MODEL_GENERATE` | генератор заданий | `gpt-5.6-sol` |
| `EDUKATOR_MODEL_VALIDATE` | проверяющий | `gpt-5.6-sol` |
| `EDUKATOR_MODEL_DISPUTE` | разбор спора | `gpt-5.6-sol` |
| `EDUKATOR_MODEL_INTEGRITY` | проверка осмысленности ответов занятия | `gpt-5.6-sol` |
| `EDUKATOR_MODEL_CURRICULUM` | сборка карты тем | `gpt-5.6-sol` |

Пустая переменная считается незаданной. Запасная модель — `gpt-5.6-terra`,
автоматического отката на неё нет: её выбирают руками этими же переменными или
флагом `--model`. Флаг сильнее переменной и в `prefetch` накрывает обе роли
сразу — генератора и проверяющего.

### Опережающее наполнение

```sh
npm run prefetch -- --child <id>                        # погреть ближайшие темы плана
npm run prefetch -- --all                               # всех готовых детей подряд
npm run prefetch -- --child <id> --topics 12 --target 10   # шире и глубже
npm run prefetch -- --child <id> --target 10 --threshold 10  # добрать и уже тёплые темы
npm run prefetch -- --child <id> --export               # выгрузить банк в посевные файлы
```

| Флаг | Значение по умолчанию | Смысл |
|---|---|---|
| `--topics` | `3` | сколько ближайших тем плана греть |
| `--target` | `8` | до какого запаса заданий доливать тему |
| `--threshold` | `4`, но не выше `--target` | остаток, ниже которого тема доливается |
| `--batches` | `4` | потолок батчей на тему за цикл |
| `--cycles` | `1` | сколько циклов подряд; между ними план пересчитывается |
| `--model` | модели ролей `generate` и `validate` | одна модель сразу на генератор и на проверяющего; чтобы развести их, задавайте `EDUKATOR_MODEL_*` |
| `--export` | выключено | выгрузить содержимое банка в посевные файлы |
| `--out` | `content/seed-bank` | куда выгружать посев |
| `--seed-dir` | `content/seed-bank` | откуда подгружать посев перед наполнением |
| `--child` | — | какого ребёнка греть; задать нужно ровно один из `--child` и `--all` |
| `--all` | выключено | греть всех готовых детей подряд; с `--export` несовместим |
| `--data-dir` | `data` (или `EDUKATOR_DATA_DIR`) | каталог данных: `control.db` и базы детей |
| `--curriculum` | `content/curriculum` | карта тем |

Прогрев берёт замок каталога данных и при живом сервере отказывается: предел
одновременных вызовов codex процессный, и второй процесс на том же каталоге
завёл бы вторую пару слотов — занятие ученика встало бы в очередь за прогревом.
Кого греть, указывается всегда: баз столько, сколько детей, и умолчание грело бы
очередь не тому ученику, который сядет заниматься.

Тема доливается, только пока её остаток ниже порога, поэтому один поднятый
`--target` уже тёплые темы глубже не копает — поднимайте вместе с ним и
`--threshold`. Прогон, в котором codex не запустился, не долилась ни одна
голодная тема либо сорвалась выгрузка посева (`--export`: посев предмета не
прочитался, банк пуст, исходного снимка нет при существующем файле назначения,
снимок не собрался или файл не записался), возвращает код 1: `prefetch`
зовут из `&&`-цепочек, и «0 новых заданий» с нулём на выходе там неотличимо от
«всё уже тёплое». Цикл, в котором не долилась ни одна голодная тема, обрывает и
остальные циклы: следующий пошёл бы по тем же темам с тем же результатом.

На `--all` отказ одного ребёнка не отменяет прогрев остальных: он называется
отдельной строкой в stderr и попадает в код возврата, а обход идёт дальше —
`--all` зовут перед занятием всей семьи, и пропавшая база одного не повод
посадить остальных за пустой банк. Каталог данных обязан существовать: без
управляющей базы `prefetch` и `parent` отказываются, а не заводят пустую рядом с
опечаткой в `--data-dir` — иначе «нет ни одного готового ребёнка» читалось бы как
«на этом сервере нет детей». Из скриптов первую управляющую базу заводит только
`npm run parent -- create` — сервер заводит её и сам при первом запуске.

Тот же цикл умеет крутиться фоном: `runWarmupCycle` из
`server/codex/worker.ts` держит **не менее 8** провалидированных заданий на
активную тему, запускает долив при падении **ниже 4** и не ведёт больше **двух**
тем одновременно (предел считается по темам, а не по вызовам: пополнение темы —
это генератор и проверяющий подряд). Активные темы берутся у планировщика, плюс
темы незакрытых сегодня забегов, начиная с самого свежего. Завершённые забеги
исключаются: иначе законченная тема перехватывала бы выдачу и прогрев у текущей.

Сам цикл греет **одну** базу и об остальных детях не знает: кого обходить,
решает диспетчер (`server/codex/dispatcher.ts`) — один фоновый цикл на процесс.
Сначала он обслуживает всех, кому не хватает до порога 4, и ученик за экраном в
этой очереди первый: ему задание нужно сейчас, а не через три чужих батча. Когда
у всех есть на чём заниматься, второй заход добивает запас до 8 и там же готовит
босса и персональный материал. Обслуживаются только дети, чья отметка активности
свежее двух суток, а будит диспетчер **браузерный** запрос ребёнка: опрос
`gate/status` агентским токеном идёт раз в двадцать секунд, и считая его
активностью, диспетчер держал бы каждого оснащённого контроллером ребёнка вечно
«за экраном», а получасовой отступ по недоступному codex не наступал бы вовсе.
Родитель за сводкой — тоже не ученик за экраном. Указатель обхода
переживает цикл: иначе последний ребёнок доходил бы до генерации только в те
обходы, где всем предыдущим уже нечего греть.

Расход фона ограничен **суточной квотой в 60 вызовов codex на ребёнка** по
московским суткам. Считаются настоящие запуски модели, включая неудачные:
зацикливание на ошибках иначе обходило бы саму защиту. Спор и проверка ответа
квотой не трогаются — они идут по живому запросу ученика. Исчерпанная квота
останавливает фон этого ребёнка до полуночи и не мешает остальным.

Сервер запускает диспетчер в `onListen`, останавливает в `onClose` и перед
закрытием SQLite дожидается текущего цикла и фоновых разборов споров.
`npm run prefetch` остаётся ручным/offline-путём прогрева и экспорта посева.

### Персональные материалы в фоне

В конце каждого прохода — после обычного банка и подготовки босса — воркер
обслуживает очередь персональных разборов. Материалы появляются только по
откалиброванным предметам: после завершённого триажа либо когда обычные
завершённые забеги покрыли до 12 разных экзаменационных тем. Среди незакрытых
пробелов выбирается лучшая тема каждого предмета тем же `isGap` и ранжированием
планировщика; одновременно
готовятся максимум три материала и не больше одного живого на предмет.

Подготовка проходит целиком до публикации: воркер атомарно создаёт
`preparing`, строит теорию по карте темы, предпосылкам, интересам и пяти
последним ошибкам, независимо проверяет её, затем генерирует и проверяет пять
самостоятельных вопросов по содержимому. Все вызовы используют роли
`generate`/`validate`, общий двухслотовый бюджет и общий backoff. Зависший
`preparing` старше 30 минут переводится в `rejected` перед новым claim. Карточка
`ready` становится видна лишь одной транзакцией вместе со всеми пятью
`lesson_reserved` заданиями и их порядком.

Если неоткрытая тема перестала быть пробелом, материал получает `retired`, а
его тестовые задания возвращаются в обычный валидный банк. Незачёт оставляет
тот же материал `active`, сохраняет его теорию и пять заданий и не позволяет
воркеру подменить его новой подачей. После перечитывания создаётся следующий
пронумерованный запуск; повторы продолжаются до зачёта.

### Посевной банк

`content/seed-bank/{math,russian,english}.json` — снимок банка, лежащий в
репозитории: math 52 задания, russian 57, english 47, по 6 тем на предмет. Он
закрывает два случая: первый запуск, когда база пуста, и откат, когда codex
недоступен и воркеру нечем пополнить очередь.

Посев заливается один раз за старт сервера и ещё раз лениво — когда очередь темы
оказалась пуста, `takeTaskOrSeed` дозаливает посев по предмету этой темы и
повторяет выдачу. Отметки «посев загружен» нет намеренно: повтор отсекается тем
же отпечатком формулировки, что и дубли генератора, поэтому дописать в посевной
файл новую тему можно в любой момент, а флаг разошёлся бы с содержимым базы.

Порча посевного файла занятие не роняет ни при старте, ни посреди него: причина
уходит в stderr, а тема выглядит просто пустой — что и есть правда.

Посев проходит через тот же `parseTaskBatch`, что и ответ модели: файл в
репозитории — такой же непроверенный текст, просто сгенерированный раньше. Тема
вне карты роняет разбор, а не молча пропускает задание.

Обновляется посев выгрузкой из банка:

```sh
npm run prefetch -- --child <id> --topics 18 --cycles 3 --export
```

Выгрузка берёт задания темы, включая выданные, а порядок тем — из карты: иначе
каждый экспорт давал бы бессмысленный диф. Не берутся только отбракованные
(`rejected`): негодным их пометило само занятие, и вернувшись в посев такое
задание либо снова уехало бы ученику, либо уронило бы разбор файла и оставило бы
без посева весь предмет.

Пишутся посевные файлы через соседний временный, как и карта тем: оборванная на
середине запись оставила бы вместо снимка битый JSON.

### Когда codex недоступен

Ничего не падает. Воркер ловит `CodexUnavailableError`, пишет в лог и уходит на
паузу, удваивающуюся с каждым подряд идущим отказом: минута, две, четыре — до
получаса. Так же он поступает с циклом, в котором была хоть одна голодная тема и
ни одна не долилась: `CodexUnavailableError` покрывает не всякий отказ модели —
процесс, стартовавший и вышедший с ненулевым кодом (просроченная авторизация,
исчерпанная квота у самого codex), приезжает обычной ошибкой, и без этой
проверки воркер запускал бы codex по каждой голодной теме раз в минуту вечно.
Исчерпанная **суточная квота ребёнка** приезжает наследником
`CodexUnavailableError`, но диспетчер её переспрашивает и в отступ не уходит:
такой ребёнок помечается пропущенным до московской полуночи, а остальные греются
дальше — один наигравшийся не имеет права остановить прогрев всей семье.
Получасовой отступ остаётся за настоящей недоступностью модели. Отказ одной темы
из нескольких отступа не включает: очередь всё-таки греется. Ручной `prefetch`
цикл не повторяет: ждать возвращения codex в
интерактивном запуске незачем. Задание ученику всё равно выдаётся — из очереди,
а когда она пуста, из посевного банка. Спорный ответ в это время остаётся
открытым и разберётся следующей попыткой, а не закрывается отказом.

### Формат задания и требования к `accept[]`

Источник правды — `schemas/tasks.json`; инварианты, которые схема выразить не
может, проверяет `server/codex/task-schema.ts`. Через него проходит и ответ
модели, и посевной файл.

| Поле | Значение |
|---|---|
| `instruction` | краткое указание, что сделать; не дублирует материал |
| `material` | отдельный текст или display-LaTeX, к которому относится инструкция |
| `material_format` | `none`, `text` или `math`; при `math` LaTeX записан без `$` |
| `choices` | для `choice` — 2–6 уникальных вариантов, один буквально равен `answer`; для остальных форматов — пустой массив |
| `answer` | эталон в самой короткой записи; для числовой темы — ровно одно число без пояснений |
| `accept` | равноправные записи того же ответа, включая `answer` |
| `hint` | 2–4 содержательных предложения: правило, применимость, путь и самопроверка; не раскрывает ответ или позицию варианта |
| `explain` | разбор по шагам, показывается после ответа |
| `joke` | короткая шутка напарника в тон подростку |
| `difficulty` | 1, 2 или 3 |

Материал пуст только при `material_format: none`; `text` и `math` требуют
непустого содержимого. `hint`, `explain` и `joke` должны содержать непробельный текст. Одни пробелы
проходят `minLength` в JSON Schema, но отвергаются `parseTaskBatch`, иначе
последующая выгрузка посева ломается на такой строке.

Требования к `accept[]`, каждое со своей причиной:

- **непуст и содержит `answer`** — иначе эталон не сойдётся сам с собой;
- **без дублей**, причём повтор ищется нормализацией того же формата, которым
  потом сверяется ответ ученика: две записи, неотличимые для нормализатора, —
  мусор, а не варианты;
- **при `answer_format: number` в записи не больше одного числа.** «45 или 46»
  неоднозначно и отвергается, а вот словесная форма без чисел («сорок пять»)
  допустима: она сверяется как текст. Запретить её нельзя — разбор спора
  дописывает в `accept[]` подтверждённый ответ ученика как есть, а выгруженный
  посевной файл читается обратно этим же разбором, и запрет ломал бы задание
  навсегда после первого же спора. Сам `answer` при этом обязан читаться как
  ровно одно число, и проверка идёт до сверки `answer` с `accept[]` именно
  поэтому: эталон без числа уронил бы нормализатор. Генератору словесные записи
  всё равно запрещены промптом — они нужны только как след спора;
- **при `answer_format: number` в записи нет знака процента.** «40%»
  нормализатор читает как долю 0,4, так что рядом с ответом «40» такая запись
  засчитывала бы ответ, ошибочный ровно в сто раз, и поднимала бы за него
  `mastery`. Ученику она при этом не нужна: два прочтения «40%» разворачиваются
  только для ответа ученика, и написанное им «40%» проходит по самому `answer`.
  Запрет действует и на разбор спора — иначе дыра открывалась бы уже в рабочей
  базе, а не при генерации;
- **`hint` не содержит `answer`** — совпадение ищется по границе слова, но не
  через `\b` (она в JS только ASCII, а ответы бывают русскими словами), поэтому
  подсказка «начни с 450 монет» не считается раскрывающей ответ 45.

Практически `accept[]` — это главное место, где решается, засчитается ли верный
ответ ученика. Наполнять его нужно всеми записями, которые ребёнок реально
наберёт: сокращениями («несов. вид» рядом с «несовершенный вид»), номером без
слова («II» рядом с «II спряжение»), словом и цифрой. Числовые вольности
нормализатор берёт на себя сам — юникодный минус, запятую как разделитель,
пробел между разрядами, дробь, — а текстовые не берёт никто.

### Проверка сгенерированного

Проверяющий получает структурированные условия и подсказки, но не эталоны и
разборы. Он решает каждое задание сам и отдельно оценивает пять вещей — условие
однозначно, ситуация не натянута, тема заявленная, содержание уместно подростку.
Поле `hint_safe` отдельно подтверждает, что подсказка не сообщает ответ прямо,
позицией варианта или однозначным пересказом.
Ответ проверяющего сверяется с эталоном генератора нормализатором, а не строкой:
«−45» и «-45 монет» — один и тот же ответ. Расхождение ответов или любая
проваленная оценка отправляют задание в отбраковку с записанной причиной.

Персоны и профиля ученика в промпте проверяющего намеренно нет: общий с
генератором контекст толкает проверяющего согласиться с уже написанным.

## Подводные камни

Здесь собрано то, что стоило времени при реализации и не выводится из кода.

**codex висит без EOF на stdin.** Вызов передаёт prompt через stdin и сразу
закрывает поток: без EOF процесс ждёт ввода, а prompt в argv на полном учебнике
упирается в Linux-лимит одного аргумента и даёт `E2BIG`. OCR ограничен 5
минутами, каждый вызов codex — 10 минутами, а общий stdout+stderr процесса — 1
МиБ. `--attempts 3` поэтому допускает до 30 минут работы модели. CLI-флагов для
этих лимитов пока нет. Зависший процесс и вся его группа завершаются; это
проверяется исполняемыми заглушками.
Полная форма вызова:

```sh
codex exec --ephemeral --ignore-user-config --ignore-rules \
  --skip-git-repo-check --disable shell_tool --disable unified_exec \
  --sandbox read-only --cd <временный-каталог> \
  -m gpt-5.6-sol \
  --output-schema <schema> -o <out> -- - <<'PROMPT'
<промпт>
PROMPT
```

**`--output-schema` не принимает половину JSON Schema.** Запрос падает с
`invalid_json_schema` ещё до обращения к модели на `uniqueItems`, `minItems`,
`pattern`, `minLength`, `minimum`/`maximum` и родственных ключевых словах.
Поэтому в рабочий каталог кладётся очищенная копия схемы, а ограничения не
теряются: их проверяет разбор ответа, диапазоны продублированы словами в
`description` и в промпте. Чистка (`codexOutputSchema`) общая для всех четырёх
схем — `curriculum.json`, `tasks.json`, `verdicts.json`, `dispute.json`, — и
рукописной копии ни одной из них в коде нет. Режет она только ключевые слова:
ключи под `properties` — это имена полей данных, и поле, названное `pattern` или
`minimum`, осталось бы в `required`, сделав схему невыполнимой.

**Корень схемы — объект, а не массив.** Структурированный вывод допускает в
корне только объект, поэтому и карта, и батч заданий имеют вид
`{ ..., items[] }`, а не голый массив.

**Legacy-учебники — сканы без текстового слоя.** `pdf-parse` вытаскивает из
818 страниц ноль символов, так что добавлен откат в OCR:
`scripts/ocr-pdf.swift` рендерит страницу через PDFKit и распознаёт системным
Vision (`ru-RU` + `en-US`). Vision выбран потому, что он уже есть в macOS,
знает русский и не требует ни установки, ни языковых моделей; плата — эта ветка
работает только на macOS. `pdf-parse` остался первой попыткой: для PDF с
текстовым слоем OCR не запускается.

**OCR читает оглавление по колонкам.** Сначала идут все названия, потом
отдельным блоком все номера страниц, а «§» часто распознаётся как «$».
Эвристика «название … номер страницы» на таком тексте не работает, поэтому
пункт опознаётся тремя способами: отточие с номером в конце строки, маркер
раздела в начале строки и голый номер страницы. Голые номера засчитываются
только там, где уже есть хотя бы три названия, — иначе страница «Ответы к
упражнениям» выглядит идеальным оглавлением.

**Оглавление бывает в конце книги** (математика — с. 333, русский — с. 314-318,
английский — с. 3), а OCR страницы стоит около секунды. Поэтому просматриваются
не все страницы, а по 15 с каждого края.

**`\b` в JS работает только с ASCII.** `/^глава\b/` не находит границу после
кириллицы и не срабатывает никогда. Граница слова выражена через `(?!\p{L})`.

**Внешние ключи SQLite включаются на каждом соединении.** `PRAGMA
foreign_keys = ON` живёт в `openDatabase`, а не в схеме; открытие базы мимо
`openDatabase` молча отключает всю ссылочную целостность.

**Ajv кладёт виновника ошибки в `params`, а не в `message`.** Текст ошибки
собирается из обоих — иначе «must NOT have additional properties» не говорит,
какое поле лишнее.

**Темы с многочастным текстовым ответом дают вдвое меньше батча.** Проверяющий
массово отбраковывает разборы, где ответ состоит из нескольких частей: он пишет
«несовершенный вид», генератор — «несов. вид», и это честное расхождение —
ученик наберёт как придётся. Кодом это не лечится, лечится составом `accept[]` и
формулировкой `prompt_seed`. Половину потерь снял переход на `gpt-5.6-sol`: он
сам перечисляет сокращения в `accept[]`. Остальное по-прежнему на составе
посева — закладывайтесь на такой выход при его сборке.

**Порог долива считается по остатку, а не по запасу.** Тему с 4-5 заданиями
воркер не трогает вовсе, поэтому «догнать предмет до 30 заданий» повторными
циклами по тем же темам не выйдет: цикл честно решит, что делать нечего. План
берут шире (`--topics 18`), и добор идёт новыми темами.

**Отпечаток формулировки обязан различать числа и знаки действия.** Он
нормализует пробелы и регистр, но не цифры и не знаки: склеив «2+2» и «3+5» — или
«4800 : 16 + 37 · 25» и «4800 · 16 - 37 : 25», отличающиеся только знаками, —
банк отверг бы всю тему как дубли после первого же задания. Синонимы одного
знака («·», «×», «*») при этом сводятся к одной записи.

**Дубли отсекаются базой, а не чтением перед вставкой.** Воркер пополняет две
темы одновременно, и между «прочитали отпечатки» и «записали» пролезал бы тот же
вопрос. Вставка идёт `ON CONFLICT DO NOTHING`, выдача — одним
`UPDATE ... RETURNING` по той же причине: выбор и пометка врозь дали бы двум
забегам одно задание.

## Решения ядра

**Юникодный минус.** `−45` и `-45` — одно и то же число; туда же запятая как
десятичный разделитель, дробь `a/b` (тема `math.common-fractions` объявлена
как `number`, иначе «3/4» не сошлось бы с «0,75») и пробел между разрядами
(«45 000» — одно число, но «12 3» — два). Причина отказа возвращается отдельным
полем (`mismatch`, `empty`, `no-number`, `ambiguous-number`): разбору спора
через LLM на этапе 3 нужно различать «не то число» и «в ответе их два».
Неоднозначный эталон в задании — дефект банка, а не ошибка ученика: проверка
падает с внятной ошибкой и делается до проверки ответа.

Ответ со знаком процента читается двояко: «45%» подходит и к эталону `0.45`, и к
эталону `45`. Эталон темы `math.percent` записан самим числом («Сколько
процентов класса составляют девочки?» — «40»), и одно только деление на сто
засчитывало бы верный ответ неверным. Обратной симметрии нет намеренно: голое
«50» при эталоне `0.5` остаётся другим числом, а не второй записью того же.
Из-за этой односторонности сам эталон числовой темы со знаком процента запрещён
(`parseTaskBatch`): «40%» в `answer` нормализатор прочёл бы как долю 0,4, и
ученик, написавший требуемое формой ответа «40», получил бы незачёт на верном
ответе. Промпт генератора требует голого числа.
Выбор (`choice`) показывается вертикальной radio-группой с визуальными метками
A/B/C. Клиент отправляет полный текст выбранного варианта, поэтому метка не
является ответом и не добавляется в `accept[]`; `answer` обязан буквально
совпасть с одним из `choices`.

**Уверенность затухает непрерывно.** Хранимая `confidence` свежа только на
момент `last_seen`, текущее значение считает `confidenceAt(state, now)`.
Побочный эффект: после долгого простоя тема выпадает из «пробелов» — данных о
ней фактически больше нет.

**Подсказка ослабляет только рост.** Неверный ответ с подсказкой падает на
общих основаниях: подсказали, и всё равно мимо — это худший сигнал, а не
смягчающий.

**Предметы не чередуются строго.** Строгое чередование по максимуму отставания
выбрасывает третий предмет из плана совсем. Спрос предмета считается как
`SUBJECT_FLOOR + deficit` и делится на число уже назначенных ему забегов: при
отставании 1.0 / 0.5 / 0.2 план из шести забегов даёт 3 / 2 / 1 без двух
забегов подряд по одному предмету.

**«Считать не из чего» — это `null`, а не двойка.** Карта без тем с
`exam_weight ≥ 1` означает «неизвестно»; оценка 2.0 была бы утверждением о
ребёнке, которого никто не проверял.

## Решения генерации

**Всё, что пришло не от нас, уходит в промпт блоком JSON.** Интересы вводит
человек, прошлые формулировки и замечания неудачной попытки приходят от модели —
ни то, ни другое не должно уметь открыть собственный раздел промпта и
переопределить инструкции. Разделы промпта пишет только код.

**Персона — один файл на всё приложение.** `content/persona.md` читается до
первого вызова codex: его отсутствие — ошибка настройки, и узнать о ней дешевле
сразу, чем через полминуты работы модели. Промахивается тон напарника — правится
это в одном месте.

**Задание уезжает в браузер без ответа.** В выдаче нет `answer` и `accept[]` —
и заодно нет `explain` и `joke`: разбор пересказывает решение целиком, то есть
содержит тот же ответ другими словами. Всё это ученик получает ответом на
задание.

**Подтверждённый спор пересчитывает модель заново.** Обратным сдвигом это не
чинится: `mastery` меняется цепочкой, и после спорной попытки успели пройти
другие. Источник истины пересчёта — таблица `attempts`.

**Вызов модели никогда не идёт внутри транзакции.** Он занимает минуты и под WAL
держал бы запись. Спор перечитывается уже под записью: параллельный разбор мог
закрыть его, и второй вердикт удвоил бы `accept[]`.

## Что делает человек

Автоматические тесты проверяют структуру карты тем и инварианты заданий, но не
их содержание. Ниже — ручные release-гейты реализованного этапа 3 перед этапом 4.

**Legacy-карты тем.** При изменении bootstrap-файлов прочитать
`content/curriculum/*.json` и убедиться, что темы и `prompt_seed` осмысленны.
Туда же — правка `exam_weight` по известным слабым местам конкретного ребёнка
и точная дата экзамена в профиле: от неё считается рост срочности тем.
Отдельно — прогнать планировщик на заполненной карте и вручную проверить первые
10 тем. Жёсткий `prereqs_ready` уже заменён мягким множителем; остаётся решить,
как поступить с тяжёлыми темами, которые не помещаются в 40-часовой бюджет при
50-60% верных ответов. Если формула меняется, сначала обновляется утверждённая
спека.

**Выборка заданий.** Прочитать по 10 заданий каждого предмета из
`content/seed-bank/` и оценить: условия осмысленны, юмор попадает в возраст,
разборы понятны. Проверяющий ловит расхождение ответов и натянутые ситуации, но
вкус проверить не может. Промахивается тон — правится `content/persona.md`.

**Профиль.** Указать имя ребёнка и интересы. До этого генерация работает на
нейтральном умолчании (`видеоигры`, `ютуб и стримы`, `школьные будни`) и сюжеты
будут пресными — а это единственное, что реально отличает «задачи из его мира»
от обычного задачника.

**Тон интерфейса.** Вычитать знакомство, главный экран, забег и финал живым
человеком: подписи должны звучать как напарник из `content/persona.md`, без
снисходительности и канцелярита.

**Живой проход.** Дать ученику пройти полный забег из 12 заданий и записать,
не утомляет ли темп, понятны ли переходы и читается ли финальный экран как итог.

**Многоарендность на живой машине.** Пройти путь целиком: завести родителя
скриптом, поставить пароль по ссылке, завести ребёнка, выпустить ссылку
устройству и погасить её на втором компьютере, провести забег. После
`npm run adopt` сверить перенесённый прогресс с оригиналом — XP, темы, прогноз,
ленту забегов — и только потом убирать `edukator.db`. Контроллер Family Safety
с агентским токеном проверяется на живом Microsoft-аккаунте: тесты его не
вызывают.
